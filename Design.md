# XMemeory 设计方案

## v0.4.1 — 精确图文导出与搜索定位

状态：用户已明确要求开发。仅调整当前笔记导出及当前正文搜索定位，不增加历史版本搜索。

- 导出只包含当前 Markdown 与其中直接引用的本地图片，保留目录关系；不递归导出链接笔记，不携带无关附件。支持标准、引用式图片及 Wiki 图片；远程图片保持链接，不联网下载。缺失或越界的本地图片明确报错。
- 点击全局搜索结果，在最新正文中重新查找关键词并滚动选中；优先富文本，源码语法命中时切换源码定位。仅标题或元数据命中时明确说明正文无匹配，不修改正文。
- 使用现有 Markdown 解析器及编辑器，不新增服务或持久化格式。测试覆盖附件排除、路径安全、重复引用、代码示例排除及真实窗口搜索定位、重复点击与保存状态。
- 完成类型检查、核心测试、构建及桌面专项后记录 ChangeLog，并按项目要求提交代码与 v0.4.1 标签。

## v0.4.0 — 首次代码仓库提交

状态：2026-09-22 用户明确要求提交至 `https://github.com/Andrew-liu/XMemory.git`。

- 范围：现有源码、测试、配置及说明；初始化 `main`，创建附注标签 `v0.4.0`，推送指定代码仓库。无新增功能或数据迁移。
- 排除依赖、构建产物、个人资料库及凭据。验收包含类型检查、全量测试、构建、暂存内容检查和远端分支及标签核对。
- 身份或认证缺失时保留本地准备，不宣称发布成功。

## v0.4.0 — 跳过未变化文件下载

状态：用户要求“实现一下”，按既有同步范围优化，不改变同步方向或冲突处理规则。

执行结果：本地实现完成，类型检查、107 项单元测试、生产构建和真实 Electron/Windows 打包 EXE 同步专项通过，详见 ChangeLog。真实私库与 Mac 未验证。

- 每次仍读取仓库、分支、提交与目录树，以远端 Git blob SHA 标识实际文件内容。先计算本地文件的 Git blob SHA；与远端一致时直接复用本地字节，不请求 blob。
- 本地有修改或文件缺失时，尝试读取本机按 blob SHA 保存的远端缓存，验证哈希后使用；缓存不存在或损坏则下载并校验。不能仅凭路径或旧同步基线认定内容相同。
- 缓存保存在资料库 `local/sync-cache/<仓库分支标识>/`，不参与同步；下载/上传成功取得的字节可缓存，基线仍只在远端分支更新成功后写入。成功同步后回收本缓存目录不再使用的 blob，失败保留可复用缓存。
- 保持编辑脏状态保护、删除标记、合并和冲突链策略。首次使用兼容旧基线；删除缓存不会删除正文，只会触发重新下载。没有用户正文格式迁移。
- 状态展示本轮下载、复用本地、命中缓存的文件数，帮助确认优化是否生效。不跳过远端目录检查，不以“分支未变化”忽略本地修改。
- 验收：第二轮零 blob 下载、本地单端修改、远端单文件变化、双端冲突、缓存缺失/损坏、下载完整性失败、提交失败后重试、图片与文本及编辑保护回归；类型检查、全量测试、生产构建、Windows 测试包。仅使用合成 API，不写真实私库。

## v0.4.0 — 完整多模型切换

状态：2026-09-21 用户确认“可以，开发”。已实现，本地功能验证完成；Windows 测试包验收记录见 ChangeLog，尚未发布。

### 目标与范围

- 当前设置可保存多个服务，但创作 UI 和主进程固定查找 deepseek，导致“当前服务”选择不生效。本轮打通配置、切换、实际调用、凭据隔离和候选稿追溯。
- 支持现有 DeepSeek 适配器及 OpenAI Chat Completions 兼容 API。每份配置独立保存名称、Base URL、模型名称和 API Key；同一家服务可建立多份模型配置。
- 设置页支持新增、编辑、选择和删除配置；至少保留一份配置。删除当前配置时明确切换到剩余配置，并删除该配置本机保存的 Key，不影响已有候选稿。
- 灵感创作面板增加模型选择器，与设置页的当前选择一致；显示本次实际使用的服务和模型，生成期间禁用切换。
- 设置中未保存的内容不静默丢弃：切换或新增配置前先保存当前编辑，保存失败则保持原选择；Key 留空保持原值。没有 Key 或模型名称时明确提示，不回退到 DeepSeek。
- 当前需求不包含原生 Anthropic Messages、Gemini 或 Responses 协议适配，也不自动拉取模型目录。若通过兼容网关提供 Chat Completions，可按兼容服务配置。
- 继续支持短推/串文、素材、停止生成与现有联网工具。工具调用需要所选模型支持；服务不兼容时明确报错，不静默换模型或另发请求。

### 流程与模块

1. 用户新增服务，填写名称、HTTPS Base URL、模型 ID 和 Key，保存并选中。
2. 返回灵感，在创作面板查看或切换当前模型，点击生成。
3. 主进程在请求开始时固定配置与 Key 快照；本次流式输出和工具步骤使用同一模型，不受后续设置变化影响。
4. 候选稿保存服务 ID、服务名称及模型 ID 快照，历史记录显示生成来源，不保存 Key。

- `src/shared/types.ts`：候选稿增加可选模型来源字段；沿用 Provider 和 activeProvider。
- `src/main/index.ts`：设置校验配置 ID 唯一、当前 ID 有效、Key 仅可写入存在的服务 ID；生成使用 activeProvider，凭据独立保存并在删除配置时清理。拒绝覆盖 GitHub/Cookie 等保留凭据键。
- `src/main/ai.ts`：按配置选择现有适配器，生成保存模型来源；错误区分配置缺失与服务失败，不回显密钥和完整请求。
- `src/renderer/src.tsx`：服务 CRUD、切换前保存、创作面板模型选择、忙碌状态与历史模型来源。

### 兼容与边界

- 保留现有 deepseek ID、模型配置及 Key；旧候选稿无模型来源时显示“未记录”，不推断。
- 无数据库迁移；新增来源字段属于可选 metadata，沿用既有同步。配置和凭据仍只保存在本机。
- 旧 activeProvider 无效时提示选择有效配置，不自动向其他服务发送内容；失败、停止生成及部分正文保存沿用现有行为。
- 不改动同时存在的冲突链修复和记忆采集代码，不触碰真实凭据或向实际模型发送测试正文。

### 实施与验收

1. 确认协议范围和配置交互，实施主进程校验、模型路由和 UI。
2. 合成 API 验证 DeepSeek 与两份兼容配置切换后 URL、模型 ID、Authorization 均正确；不同配置间不串 Key，缺 Key 不回退。
3. 真实 Electron 验证新增/编辑/删除、切换保存、创作选择器、流式生成、停止、候选稿来源及重启保持；非法/保留 ID 被拒绝。
4. 类型检查、直接相关及全量单元测试、生产构建、Windows 测试包；在 ChangeLog 记录真实结果。真实第三方服务与 Mac 另列未验证范围。
5. 版本计划 v0.4.0；当前目录尚非 Git 仓库，代码发布远端未配置，不推送个人数据仓库。

## v0.3.0 — 同文件冲突链收敛修复

状态：用户于 2026-09-21 明确要求修复，按本节方案实施。

- 根因：解决旧冲突时若当前文件已变化，现实现会创建新比较记录但不归档旧记录；同一文件每次同步或再次处理都会继续追加，导致冲突数量增长且旧卡片永远无法解决。
- 同一个 `noteId` 同时只保留一条活动冲突。产生新比较版本时，用“当前磁盘文件 + 最新待合并版本”替换旧活动记录；旧记录完整保存到 `local/` 恢复记录并写入可同步的 `superseded` 标记，不删除任何版本。
- 资料库刷新时自动收敛历史重复记录：按 `noteId` 分组，保留最新有效比较；其余记录归档。若保留记录中的版本 A 已落后于当前磁盘文件，则自动重建为当前文件与版本 B 的比较，确保按钮可操作。
- “采用 A”表示保留当前磁盘文件并一次性解决该文件全部历史冲突；“采用 B”“两份都保留”和“手动合并”只作用于页面当前展示的最新版本 B，成功后同时归档该文件被取代的冲突链。
- 并发边界：用户点击处理后若磁盘再次变化，不覆盖新内容；页面自动刷新为新的单条比较并提示重新确认，不再追加卡片。所有被取代记录仍可从 `local/` 恢复。
- 验收：构造同一文件 7 条冲突且含多个 current/incoming 哈希，刷新后只显示 1 条；采用 A/B/两份保留/手动合并均可完成；处理期间再次外部修改只刷新同一张卡；连续同步不再重新生成已取代冲突。

## v0.3.0 — 同步编辑竞态与冲突假死修复

状态：用户于 2026-09-21 明确要求修复。

- GitHub 同步完成远端读取后、开始刷新或写入本地文件前，再次检查编辑器是否存在未保存内容；若有则安全停止本轮同步，不改写本地或远端，提示保存完成后重试。
- 编辑器保存发现外部修改时，双方内容继续写入冲突记录；随后将编辑器切回磁盘当前版本、清除待保存状态并解除全局导航锁，使用户能直接进入“冲突”页面。
- 冲突发生前的编辑器版本不得丢失，必须可在冲突版本 B 中查看和选择；普通权限、磁盘及格式错误仍保持未保存状态，不得误判为已保留。
- 验收覆盖：同步落盘前发现编辑中内容时零本地写入；外部修改与编辑器保存竞态产生冲突后，原文件保持外部版本、冲突保留编辑版本、界面可继续导航并打开冲突页。

## v0.3.0 — 完成时间差异自动合并

状态：用户于 2026-09-21 确认调整。

- 当双方 Markdown 正文、完成状态及除 `completedAt` 外的全部 Front Matter 等价，且双方均为已完成、时间戳均有效时，不创建待处理冲突，自动采用较早的 `completedAt` 作为共同版本。
- 若正文、`completed`、标题、日期、自定义字段或任意其他字段不同，仍按真实冲突处理；不忽略缺失或无效的完成时间。
- 新同步直接合并并将共同版本写回本地及远端快照；已有同类冲突在刷新时先保存恢复记录，再写回较早时间并标记已解决。
- 验收覆盖较早时间来自任一端、已有冲突自动归档，以及正文、标题、完成状态差异不被误合并。

## v0.3.0 — 启动时自动同步

状态：用户于 2026-09-21 明确要求实现；在现有 GitHub 私库同步范围内开发。

- 应用每次完成资料库与窗口初始化后检查 GitHub 同步配置；仅当仓库地址和本机加密保存的 Token 均存在时，后台主动执行一次完整同步。
- 启动同步不阻塞窗口打开；失败沿用同步状态展示，不弹出额外阻塞对话框，也不影响本地编辑。未配置完整时不发送 GitHub 请求。
- 启动同步与手动同步、既有每 10 分钟同步共用 `GithubSync.running` 单实例保护；启动后重置定时基准，避免刚完成启动同步又立即触发定时同步。
- 不增加自动重试、不修改同步冲突策略、资料格式或凭据保存方式。验收覆盖完整配置触发一次、缺少仓库或 Token 不触发、失败被安全收尾，并执行类型检查、完整单元测试和生产构建。

## v0.3.0 — 相同正文冲突判定与展示修复

状态：用户要求修复相同 diff 仍显示冲突，按既有同步与冲突处理范围实施。

执行结果：实现完成；类型检查、78 项单元测试、生产构建及真实 Electron/Windows 打包 EXE 冲突专项通过，详见 ChangeLog。真实私库未操作。

- 整份 Markdown 字节哈希用于三方基线，但双方正文和全部 Front Matter 值相同（仅 CRLF/LF、BOM、YAML 键顺序/表示差异）时视为等价，采用本地字节，不新增冲突。不忽略正文空格、日期、标签、完成状态或未知自定义字段。
- 新同步冲突保存双方原始 Markdown，展示正文与附加字段差异；仅格式差异不误报正文。纯空白差异使用可见标记提示。旧记录未存完整 Front Matter 时提示信息不完整，不能仅因正文相同自动丢弃。
- 历史冲突只有在原始 Markdown 可证明等价或双方完整文件哈希相同、且当前磁盘版本仍对应其当前版本时才归档；先保存 local 恢复副本，再写可同步的 resolved 标记。未知或有效差异继续保留。
- 采用 B / 保留双方时保留该版本完整 Front Matter，防止展示字段差异却只替换正文。兼容旧冲突记录，无批量正文迁移。
- 验收：等价换行/YAML 不冲突、正文与元数据真实差异不吞掉、归档旧等价记录保留恢复副本、旧不完整记录继续保留、采用 B/双方保留自定义字段、Electron 冲突 UI 展示、类型检查、全量测试、构建与 Windows 测试包。真实私库与 Mac 未验证。

## v0.3.0 — 同步连接中断修复

状态：用户报告检查连接 terminated，并要求“继续执行”；在已确认的私库同步与诊断范围内修复，不扩展同步功能。

执行结果：本地实现、73 项单元测试及真实 Electron/Windows 测试包专项验收完成，详见 ChangeLog。本机公开 API 两通道均成功，原始断线原因未复现；真实私库待用户验证。

- 目标：身份检查、仓库检查、文件传输发生超时或响应流中断时，能定位步骤并从短暂断线恢复。
- 技术：主进程为 GithubSync 注入专用 Electron session.fetch，使用系统网络配置，禁止重定向，省略 Cookie，不读取浏览器登录态；保留 Token 授权。请求地址仅允许 api.github.com 的 HTTPS 默认端口。
- 请求及响应 JSON 读取纳入同一错误边界。GET 网络中断最多重试两次，间隔 1/2 秒，每次 30 秒；写入等待 60 秒且不自动重试，避免提交结果不确定时重复写入。HTTP 401/403/404 保留原有说明，不按网络错误重试；错误不回显凭据、响应正文或代理地址。
- 状态逐步显示身份、仓库、分支、提交、目录、文件读取及上传，断线提示说明未收到完整响应，不能据此断定 Token 无效。写入失败提示远端可能已接收，需重新检查同步。
- 不修改数据格式、同步间隔、冲突策略；本轮不新增文件缓存或增量下载算法。没有数据迁移。
- 验收：合成网络模拟请求失败、响应流 terminated、超时、恢复、HTTP 401 不重试、写入不重试及敏感信息不回显；真实 Electron 设置页通过专用网络通道验证凭据保存和错误提示；类型检查、全量单元测试、构建和 Windows 测试包。真实账号及 Mac 不以模拟结果替代。

## v0.3.0 — 长文选题清单（2026-09-20）

状态：已确认，2026-09-20 用户回复“开始开发”。确认一个选题对应一条待办，大纲和长文放在同一篇 Markdown 文档。已实现并完成本地功能测试及 Windows 打包验收；尚未发布。用户要求新增“选题”入口，用待办清单记录长文灵感，打开后编写大纲和长文，已写完划线，未使用项排在已写项前面。

### 背景与目标

灵感目前按周/月组织短推想法；长文需要独立的选题清单和较长写作周期。新增选题与灵感、记忆并列，继续采用 Electron 桌面和本地 Markdown。

### 功能范围与非目标

- 侧栏顺序为“灵感、记忆、选题”，选题页显示全部未删除条目，未完成分组在上、已完成分组在下。
- 一个选题是一条可勾选待办；不增加独立子任务。
- 提供输入标题并按 Enter 快速创建，以及“新选题”按钮；非空标题即可建立选题。标题点击打开正文，复选框只切换完成状态。
- 已写完勾选后标题划线，移动到已完成分组；取消勾选后回到未完成分组。仍可继续编辑已完成正文。
- 同一完成状态内默认按创建时间倒序排列，以 ID 做稳定兜底；编辑正文不使选题跳动。不关联日历，不增加到期、催办、跨天搬移或手动拖拽排序。
- 正文默认含“## 大纲”和“## 长文”两个标题，使用同一 Markdown 文档和现有编辑器；标题可自由编辑，不自动重建或解析为两个强约束字段。已确认使用同一篇文档。
- 沿用自动保存、外部编辑回读、表格、图片预览/复制、双链、导出、回收站与冲突保留；全局搜索加入选题类型，私库同步覆盖选题及其图片。
- 本轮仅提供记录和手动写作，不把现有短推 DeepSeek 提示词用于长文，不新增长文 AI 生成或自动发布。

### 主要使用流程

1. 点击“选题”，在清单上方输入标题，Enter 创建并可选择打开。
2. 打开选题，在大纲及长文段落编辑；修改自动保存到本地 Markdown，离开前等待保存确认。
3. 写完后勾选完成，清单划线并移到下方；可撤销完成状态，正文不变。
4. 从选题清单或全局搜索返回历史内容；删除进入回收站，恢复后按完成状态重新排序。

### 技术方案、接口与数据结构

- `src/shared/types.ts`：`Kind` 增加 `topic`，复用 `Note` 的 `id/title/body/completed/completedAt/createdAt/updatedAt`；不新增日期或子任务模型。
- `src/main/library.ts`：增加 `topics/` 文件夹，正文文件为 `topics/<id>.md`；Front Matter 保存 `type: topic` 和完成状态。复用安全路径、原子写入、监听回读、SQLite 可重建索引、冲突和回收站逻辑。
- `src/main/index.ts` 与桥接：扩展 `save/search` 类型校验，现有 IPC 方法保持；AI 生成仍只接受灵感，不能误将选题作为短推来源。
- `src/renderer/`：新增选题列表与分组排序函数，接入现有编辑器、侧栏及搜索筛选。选题页的新增操作创建选题；灵感页既有日期归属行为不变。
- `src/main/sync.ts`：同步白名单增加 `topics/*.md`，保留已有 metadata、assets、tombstones 及 conflicts 策略。
- 双链解析、嵌入、导出依赖通用 Note 列表，需验证选题互链及图片相对路径均可用。

### 异常与边界处理

- 空白标题不创建；保存失败显示错误，不清空待输入标题或宣称保存成功。
- 完成状态写入失败恢复磁盘真实状态，不仅修改界面；点击勾选不触发打开或删除。
- 外部修改 `completed` 后同步更新分组；外部删除或重名冲突沿用现有恢复与冲突处理。
- 切换选题、勾选、删除前保存当前编辑，防止旧正文覆盖新值；生成仍在运行时沿用既有导航限制。
- 数据同步不把“勾选完成”当作删除，不覆盖双方冲突版本；图片共享引用保持原有文件。

### 兼容性与迁移

- 无需批量改写既有灵感、记忆和候选稿；新增目录和类型，现有 Markdown 字段及 schemaVersion 不变。
- 老版本不认识 topic 类型，不会显示或主动同步选题目录；双端使用选题需都升级，不能宣称旧版支持此功能。
- 当前本地版本为 v0.3.0，方案确认后完成实现，功能验证结果记录在 ChangeLog。项目尚未连接代码仓库，用户私有数据仓库不作为代码发布目标，不初始化或推送到未经确认的仓库。

### 阶段任务与验收

执行结果：本轮 1—5 的本地实现与验证已完成，详见 ChangeLog v0.3.0；真实私库双端与 Mac 未验证，代码仓库未配置，未发布 GitHub/tag。

1. 确认一条选题对应一条待办，以及大纲/长文的编辑组织方式。
2. 接入类型、存储、搜索、同步白名单；实现清单与编辑流程。
3. 单元测试验证未完成优先、稳定排序、类型持久化、搜索过滤和同步范围。
4. 真实 Electron 验证创建两条未完成和一条完成选题，勾选后划线下移，取消后回到前面；打开、编辑、重启后正文与完成状态保留；外部 Markdown 修改回读；删除恢复；关键词搜索选题；图片预览复制不回归。
5. 执行类型检查、适用全量测试、构建与 Windows 打包验收；记录真实测试结果后更新 ChangeLog 和 README。真实私库双设备及 Mac 验收另列未验证范围，不以 Windows 合成测试代替。

## v0.2.0 私库 Token 401 诊断（2026-09-19）

状态：用户要求继续检查 Token 401，已完成现有凭据配置和同步诊断修复，合成凭据重启验证通过；真实凭据有效性待用户检查。

- 保存前校验个人访问 Token 格式，去掉首尾空白；拒绝 Bearer 前缀、引号、隐藏字符、掩码或把名称当 Token 的输入，不输出凭据片段。
- 只读“检查连接”先调用 GitHub `/user` 验证身份，再检查私有仓库和分支。区分 Token 被拒绝、身份有效但私库不可访问、分支不可读；检查不上传内容。
- 显示 Token 类型及最近本机保存时间；说明保存不等于认证。保存新凭据时先加密写盘并验证回读，再更新内存，避免保存失败却继续使用未落盘的新值。空输入保持原值。
- 不自动重新生成 Token、不读取日常浏览器凭据、不尝试扩大权限；失效凭据仍需用户在 GitHub 重新生成并粘贴。
- 验证：格式校验与拒绝敏感回显、401/404 分阶段、只读请求范围；真实 Electron 用合成 Token 替换后重启并验证请求使用新值。类型检查、完整测试、构建、测试包；不以合成凭据测试宣称用户 Token 已有效。

## v0.2.0 图片复制到 X 的兼容修复（2026-09-19）

状态：用户反馈图片粘贴到 X 后变为相机表情，按既有图片复制范围修复。

- 当前复制同时写入 PNG、HTML 和纯文本，接收编辑器可选择不同格式。默认复制改为仅 PNG，避免文本/富文本占位内容优先；右键新增“复制图片（邮件富文本）”保留旧邮件兼容格式，预览按钮和 Ctrl+C 默认使用 PNG。
- 不更改本地图片和正文；沿用安全读取、格式和体积校验。邮件模式由主进程菜单显式选择，不扩大 IPC 参数范围。
- 验证：系统剪贴板默认仅含 PNG，真实 Chromium 粘贴事件得到 image/png 文件且无文本/HTML；显式邮件模式仍含 PNG 与 HTML；再次默认复制清除旧的富文本格式。真实 X 接收结果待用户验收，不将模拟接收器测试等同于 X 实测。

## v0.2.0 灵感图片查看（2026-09-19）

状态：用户明确要求“灵感内图片双击放大、右键复制”，已完成实现和真实 Electron 合成图片验证。

- 本地图片双击打开窗口内大图预览；默认适配窗口，可切换原始尺寸并复制，支持 Esc、关闭按钮和点击遮罩退出。
- 沿用原生右键“复制图片”；预览图也能右键复制。剪贴板继续包含 PNG 与邮件富文本 HTML，不使用文件路径替代图片。
- 只预览已经安全加载的本地图片，不自动抓取远程图片。预览、缩放、复制和关闭不修改正文或保存状态；无需数据迁移。
- 验证：真实 Electron 测试双击、适配/原图切换、Esc/遮罩/按钮关闭、右键菜单复制、正文文件不变。测试只使用合成图片。

## v0.2.0 验收修复 — 新增日期归属与跨天移动（2026-09-16）

状态：用户明确反馈并要求修复，按既有灵感日历范围完成实现与验证。

- 问题根因：顶部“新灵感”错误沿用日历当前选中日期；用户浏览或选中过昨天后，即使今天点击新增，也会写入昨天。日期工具使用本地年月日，不是 UTC 时区偏移问题。
- 顶部及侧栏通用“新灵感”始终按本机当天创建，并把日历锚点切回当天；某个日期卡片底部的快速输入仍明确创建到该日期。
- 拖拽从“仅同日排序”扩展为跨天移动：可拖到目标日已有条目的指定位置，也可拖到目标日空白区域追加到末尾。
- 跨天后写入目标 `scheduledDate`，同时将来源日与目标日的 `sortOrder` 归一化为连续序号；刷新和重启后日期与顺序保持。
- 保存失败时重新读取磁盘真实状态并报错，不保留仅界面生效的日期或顺序；不修改正文、完成状态及创建时间。
- 验收覆盖：选中过昨天后使用通用新增仍归入今天；跨天移动到空白日和有条目日期；来源日、目标日顺序连续；重启后保持；同日排序、快速创建、完成状态不回归。

## v0.2.0 验收调整 — 灵感内直接创作与私库连接检查（2026-09-16）

状态：用户明确要求移除独立创作 Tab，仅保留灵感内创作；已完成实现和真实 Electron 合成模型调用验证，外部账号联调待用户验收。

- 使用流程：打开一个灵感，点击“创作”，先保存当前标题和正文，再以此灵感调用已配置的 DeepSeek 并打开流式结果面板。无 Key 时展示配置提示，不发出请求；保留短推/串文、写作要求及联网模式，可在面板中调整后再次生成。
- 移除侧栏独立创作页以及记忆、候选稿的直接创作按钮。记忆仍能加入素材；候选稿继续保存原灵感关联，可从灵感内的候选稿列表或全局搜索访问。不会删除已有稿件。
- 标题也进入模型提示，支持只有标题的快速灵感。生成期间禁止切换编辑对象，避免结果串到另一灵感；重复点击不重复发请求。记忆和已删除内容不能作为直接生成来源。
- 私库修复：设置增加只读“检查连接”，使用保存的凭据在 App 内检查仓库和分支。失败时补充细粒度 Token 的资源所有者、仓库授权、Contents 权限提示；检查/同步前保存当前输入，避免沿用旧配置。检查成功仅代表读取权限，不能据此保证写入权限。
- 接口：新增 `checkSync()` IPC；资料格式保持不变，兼容模型设置保留，本轮灵感创作使用 DeepSeek。外部接口不自动创建仓库或分支、不读取日常浏览器凭据。
- 验证：无独立创作导航；灵感单击创作传入刚保存的标题/正文；候选稿关联及搜索；仅标题灵感、无 Key、非灵感来源、重复点击；模拟 GitHub 只读检查不写入；类型检查、单元测试、真实 Electron 交互及打包验收。真实账号权限仍由用户设置。

## v0.2.0 验收修复 — GitHub 仓库请求 404（2026-09-16）

状态：用户反馈同步 404 后的既有功能修复，本地实现、单元测试和构建完成；真实私库同步待用户验收。

- 问题：获取仓库信息的 URL 被拼成 `/repos/owner/repo/`，GitHub 对该末尾斜杠路径返回 404，正确路径 `/repos/owner/repo` 返回 200；已对公开测试仓库只读复现。
- 范围与方案：修正空 endpoint 的 URL 拼接；按仓库、分支、下载和写入阶段区分失败信息，区分无效凭据、访问不足及空仓库。不创建仓库或分支，不自动切换用户配置分支。
- 兼容性：不修改资料库格式、用户 Token 或仓库配置。只读复现不使用用户账号。
- 验收：断言仓库请求无末尾斜杠；模拟完整同步链和 401/403/404/409 失败；类型检查、单元测试、构建与打包 EXE 验证。真实私库同步由用户执行。

## v0.2.0 验收修复 — 图片本地化与仓库地址（2026-09-16）

状态：用户反馈缺陷后的修复，属于既有图片保存和私库同步范围；本地实现与自动化验证完成，真实账号验收待完成。

- 目标：修复 Chrome 能显示图片但独立请求下载失败、图片本地化后因正文变短而不更新，以及完整 GitHub URL 无法用于同步的问题。
- 方案：图片通过受管 Chrome 的独立临时标签页串行读取，沿用浏览器网络路径；只允许图片域名，设置超时和 20 MiB 限制，结束关闭标签页。正文更新不以本地路径缩短判为内容退化；已编辑正文只替换已知远程图片引用。显示附件成功/失败计数，失败保留远程引用。
- 仓库输入接受 HTTPS GitHub 仓库地址（含 `.git`）或 `owner/repo`，保存和同步前统一规范化；拒绝其他主机、凭据、查询和异常路径。分支保留用户输入，不猜测默认分支。
- 采集辅助文件 `metadata/*.capture.json` 纳入资料同步白名单，保留本地图片映射及失败状态；不扩大凭据同步范围。
- 非目标：不扩大两个采集目标，不新增书签批量采集，不创建远端仓库或分支，不读取用户 Token 进行联调。
- 兼容性：沿用现有 Markdown、图片及元数据；没有批量迁移。重新采集可更新已有远程图片引用。
- 验证：仓库地址规范化与拒绝非法输入；模拟浏览器图片响应、失败和超限；图片本地化后可重复保存、保留用户笔记；类型检查、完整单元测试、生产构建和打包后的记忆页测试。真实 X 图片和用户私库连接仍需人工验收。

## v0.2.0 创作 Agent — Jina 联网搜索与网页阅读（2026-09-15）

状态：已确认并完成开发。用户确认采用轻量实现，不依赖 Tavily、MCP 或本机额外 CLI。


### 背景与目标

现有创作链已经通过 AI SDK 提供本地记忆搜索和授权正文读取，但明确禁止联网。此次在保留现有模型配置和本地优先行为的前提下，增加基于 Jina Search 与 Jina Reader 的可选联网能力，让模型能够搜索、阅读、核验后生成带来源的候选稿。

### 功能范围与非目标

- 创作面板提供“智能联网”和“仅本地”两种模式；智能联网允许模型按任务需要调用搜索与网页阅读工具，仅本地保持现有行为。
- 联网模式通过 `https://s.jina.ai/` 搜索，通过 `https://r.jina.ai/` 读取已由本次搜索返回的 HTTPS 网页；不需要用户配置额外 API Key。
- 运行状态展示搜索词、结果数量和正在阅读的来源，不展示模型内部思考。
- 搜索或阅读失败时返回可理解的工具结果，模型继续使用本地材料生成；不因第三方搜索不可用丢失已生成正文。
- 候选稿保存本次读取的网页标题、URL、站点与访问时间；本地 `sources` 字段继续保存 Note ID，保持兼容。
- 首版不接入浏览器搜索、MCP、Agent Reach CLI、自动发布、子 Agent、付费墙或登录网页。

### 技术方案与边界

- 新增独立 Jina 客户端，统一处理超时、响应大小、重定向后的状态和错误脱敏。
- 每次 Agent 最多执行 8 步、3 次搜索、读取 5 个网页；搜索结果和单页正文均截断后再发送给模型。
- 网页 URL 必须为公开 HTTPS 地址，拒绝凭据、localhost、环回、私网和保留地址；网页读取只接受本轮搜索实际返回的 URL。
- 网页内容视为不可信引用，不能改变系统指令、工具权限或任务。
- 网络来源由主进程写入草稿辅助元数据，不将第三方正文全文持久化。

### 测试与验收

- 单元测试覆盖搜索 URL 编码、结果 URL 提取、网页白名单、私网拒绝、次数限制、超时/失败降级和正文截断。
- 类型检查、完整单元测试与生产构建通过。
- 创作面板可切换联网模式；运行状态可见；生成完成后候选稿保留实际读取来源。
- 真实 Jina 可用性依赖当前网络与第三方服务，失败时应明确降级而不是中断本地创作。

## v0.2.0 测试包体验修复 — 时间、Todo 排序、记忆入口与隐藏采集（2026-09-15）


状态：已确认并完成开发。用户于 2026-09-15 确认开始开发；第 3 项按用户修正为记忆页保留采集与暂停，并由采集按钮自动处理登录前置流程。

### 背景与目标

本轮修复四项测试体验：记忆日期应表达原内容发布时间；灵感日历中的 Todo 可手动调整顺序；记忆页采集操作更直接并自动处理登录前置条件；正常采集期间不让 Chrome 窗口干扰桌面操作。

### 功能范围

1. X 记忆新增 `publishedAt`，优先取 GraphQL 推文 `legacy.created_at` 并转换为标准 ISO 时间。记忆列表卡片和记忆编辑页日期显示 `publishedAt`；`createdAt` 继续表示首次写入本地的时间，`updatedAt` 继续表示本地最后修改时间，`capturedAt` 继续保留在采集元数据中。已有记录重新采集时，即使正文没有变化，也要补写或修正 `publishedAt`；无法从 X 响应取得发布时间的旧记录暂时回退显示 `createdAt`，不臆造时间。
2. 灵感日历同一天的 Todo 支持拖拽调整顺序。新增可持久化的 `sortOrder` 字段；拖动后只重排当前日期内的条目并立即保存，刷新与重启后顺序不变。首版不通过跨日期拖拽修改日期，日期仍由编辑或创建流程管理；键盘与复选框操作保持可用。
3. 记忆页保留“采集”和“暂停”两个核心按钮，不再单独要求用户先判断登录状态。“采集”采用统一流程：先检查专属 Chrome Profile 的登录态；已登录则直接开始采集，未登录则拉起可见 Chrome 进入登录流程，待用户完成登录并关闭登录窗口后自动检查登录态，成功后接着开始采集，无需再次点击。登录未完成、验证失败或用户关闭窗口但仍未登录时停止在明确提示状态，不开始采集。“暂停”仅在连接或采集流程进行中可用，用于终止当前流程；设置页继续保留检查登录、显示/关闭 Chrome 等诊断和管理操作。
4. 登录 Chrome 始终在屏幕内，供用户手动登录和验证。检查登录成功或开始采集后，通过 CDP 将受管 Chrome 窗口移动到屏幕外，保持有界面渲染，不使用 headless、不最小化；遇到未登录、安全验证或采集错误时恢复到屏幕内并置前。采集结束后继续隐藏，用户可在设置中通过“显示 Chrome”恢复窗口，或通过“关闭 CDP Chrome”退出。

### 数据与兼容性

- `publishedAt?: string` 和 `sortOrder?: number` 均为可选字段，旧 Markdown 与辅助元数据无需批量迁移即可读取。
- X 记忆的 `publishedAt` 写入 Markdown Front Matter，便于脱离 SQLite 仍保留原始发布时间。
- Todo 排序按同一 `scheduledDate` 内的 `sortOrder` 升序；旧条目没有顺序时按原 `createdAt` 稳定排列。首次拖动后为该日期条目写入明确顺序。
- 不改变两条固定 X URL 白名单，不扩大采集范围，不读取日常 Chrome Profile。

### 异常与边界

- X 返回无法解析的 `created_at` 时忽略该值并回退，不覆盖已有合法 `publishedAt`。
- Todo 拖拽保存失败时刷新本地真实状态并显示错误，不保留仅界面生效的假顺序。
- 正在编辑、勾选或保存的 Todo 不启动拖拽；拖动只由专用把手触发，避免点击标题误操作。
- Windows 多显示器坐标可能不同，隐藏位置按当前窗口宽度计算到虚拟桌面左侧；若 CDP 调窗失败，采集继续但显示明确提示，不使用操作系统级强制隐藏。

### 测试与验收

- X 响应中的 `created_at` 能解析为 `publishedAt`；新建和更新已有记忆都保留该时间，列表显示发布时间而非采集/修改时间。
- 同一天至少三个 Todo 可拖动换序，刷新和重启后顺序保持；勾选、打开与快速创建不回归。
- 已登录时点击记忆页“采集”直接开始采集；未登录时自动拉起登录 Chrome，登录完成并关闭窗口后自动继续采集，不要求第二次点击；未完成登录时不误采集。采集或连接过程中“暂停”能够终止当前流程。
- 登录窗口可见；检查成功和采集时窗口移出屏幕；登录失效时窗口恢复；采集仍能得到完整长文和图片。
- 执行类型检查、相关单元测试、完整测试、生产构建，以及真实 Electron 日历与记忆页交互测试。Chrome 窗口与真实 X 采集由用户环境完成最终人工验收。

## v0.2.0 — CDP 低干预书签归档（2026-09-15）

状态：方案已确认，第一阶段开发中。用户确认个人使用、书签约 500–2,000 条、首次历史归档分多天完成、采集过程不使用无头浏览器且无需人工操作、窗口隐藏运行、增量连续命中 20 条已有书签即停止、视频不下载、其他可取得内容尽量保留；其余决策采用本文推荐项。首次登录、二次验证及风控验证仍须用户在浏览器中人工完成，不能自动绕过。

第一阶段安全闸门（用户于 2026-09-15 明确确认）：只允许定向采集以下两个固定 URL，用于分别验证长文与短推的本地保存，不读取书签列表、不滚动书签时间线、不接受任意 URL，也不启用自动定时采集。旧“扫描书签”入口在本阶段替换为“两条测试内容”，直到这两条真实内容通过人工验收后才讨论放量。

- `https://x.com/xueyu1125/status/2099104432596328908`
- `https://x.com/xueyu1125/status/2099446491094175744`

### 1. 背景与目标

当前 v0.1.0 使用 Electron 持久化分区和隐藏 `BrowserWindow` 保存 X 登录态，再由应用主动重放私有 Bookmarks GraphQL。虽然已经具备分页、断点、Article 接口解析和附件补抓，但主动构造接口请求与固定限流仍偏离普通网页使用路径；Article 只解析接口中的 `plain_text` 或 `content.blocks`，缺失时不会进入 `/i/article/<id>` 核验渲染后的完整正文。

v0.2.0 改为 CDP 优先：由 XMemeory 启动安装在本机的正式版 Chrome，使用 XMemeory 独立 Profile 保存登录态，再以 Playwright `connectOverCDP()` 连接。书签发现依靠真实书签页面滚动及页面自身产生的网络响应；Article 通过真实详情页串行采集。目标是减少不必要请求和异常并发，不承诺规避 X 检测、账号绝对安全或接口永久稳定。

### 2. 确认范围

- 单个 X 账号、个人本地工具；不支持多账号并行、云端常驻采集或无人值守登录。
- 不使用 headless，不使用 Playwright 自带临时 Chromium，不使用隐身插件、指纹伪装、代理池、验证码处理服务或自动切换账号。
- Chrome 采用独立持久化 Profile。首次登录、二次验证和后续安全验证由用户在 Chrome 窗口中完成；采集开始后不需要用户滚动、点击或逐条确认。
- 正常采集时 Chrome 保持有界面并正常渲染，窗口移动到屏幕外而非无头运行。发生登录或验证问题时移回屏幕内并暂停任务。
- 首次历史回溯分多天完成；增量扫描从最新书签开始，连续命中 20 条本地已有书签后停止。
- 保存普通短推、Note Tweet、当前书签随响应返回的引用推文，以及作为书签主体的 X Article。不追抓完整推文串、回复上下文、评论区和互动数据。
- 视频不下载，保留封面、视频标记、原推链接和接口已返回的基础元数据；GIF 按视频处理。
- Article 尽量保留标题、段落、粗体、斜体、列表、引用、代码块、表格、内嵌推文、图片说明、链接文字及目标地址。广告、推荐内容、互动按钮和评论区不进入正文。
- 外部网页只保留链接与 X 已返回的卡片信息，不自动抓取站外全文。
- 本地永久保留已归档书签，不为判断取消收藏而周期性遍历全部历史。

### 3. 使用流程

1. 用户在设置中启用“Chrome CDP 采集”。XMemeory 查找受支持的正式版 Chrome，创建专属 Profile 目录，并仅监听 `127.0.0.1` 上的临时调试端口。
2. 首次连接时 Chrome 在屏幕内打开 X 书签页。用户手动完成登录和必要验证；XMemeory 不索取密码、不读取日常 Chrome Profile，也不导出裸 Cookie。
3. 登录确认后，采集窗口可移动到屏幕外。XMemeory 启动后若总开关开启、当日额度未用完且不在冷却期，自动执行一次扫描；界面始终提供暂停、继续、显示 Chrome 和彻底退出登录。
4. 扫描器监听页面自身产生的 Bookmarks GraphQL 响应，同时缓慢滚动书签页。响应作为主要结构化来源，DOM 仅用于确认页面状态、滚动进度和补充缺失字段。
5. 新书签先写入发现队列。普通推文直接归档；Article 进入独立串行队列，在详情页完成正文采集。正文落盘不等待图片全部下载。
6. 图片先记录原始 URL，后台以低并发尝试本地化；成功后使用相对路径，失败则继续保留原 URL 和失败状态。视频不进入下载队列。
7. 首次历史归档达到当日额度、连续工作时限或熔断条件后保存断点并退出采集。增量扫描连续遇到 20 条已有书签即正常结束。

### 4. CDP 与进程安全

- 优先连接 XMemeory 自己启动的 Chrome Stable，不连接用户日常 Chrome，不复制或解密日常 Profile。若 Chrome 不可用，明确提示安装或选择可执行文件；不静默退回 Electron 私有 GraphQL 采集。
- Chrome 使用专属 `--user-data-dir`，调试地址只绑定环回接口，优先让 Chrome 分配临时端口，并从专属 Profile 的 `DevToolsActivePort` 获取端点。不得使用固定公网可达端口，不在日志中输出 WebSocket 调试地址、Cookie 或请求授权头。
- 启动参数只包含 Profile、调试端口、初始书签页和必要窗口位置，不加入自动化规避或安全降级参数。沿用系统代理，不修改证书校验。
- 同一 Profile 同时只允许一个 Chrome/CDP 采集实例。启动前检查锁与已有受管进程；异常退出后先确认旧进程状态，不并发重启或强行复用被其他 Chrome 占用的目录。
- Playwright 仅负责连接、页面导航、响应监听、滚动和 DOM 提取。登录凭据及 Cookie 由 Chrome Profile 管理；XMemeory 不再把 `auth_token`、`ct0` 复制到自身 secrets 或资料库。
- v0.1.0 Electron 登录和 Cookie 导入仅作为迁移期兼容能力，默认关闭且不能与 CDP 同时采集。v0.2.0 稳定后再决定是否删除，不自动迁移裸 Cookie 到 Chrome。

### 5. 采集调度与风险控制

默认参数以保守为目标，均可在安全范围内减少额度，不能提供“无限制”模式：

- 每次滚动间隔随机约 1.8–4.5 秒，滚动距离在合理范围变化；偶尔停顿用于等待真实页面稳定，不伪造鼠标轨迹或键盘输入。
- 每发现约 20–40 条内容暂停 2–5 分钟；Article 串行处理，两篇之间等待约 8–20 秒。
- 首次历史回溯每日最多处理 200 条新书签；连续运行 30 分钟或处理 50 篇 Article 后结束，以先到者为准。按此上限，500–2,000 条预计需要约 3–10 天或更久。
- 同一时刻只允许一个 X 页面执行采集导航；图片下载并发最多 2，且不通过反复刷新 Article 页面重试。
- 自动扫描在 XMemeory 启动后触发一次，不采用固定高频定时器；当日额度、冷却时间与上次结果持久化。系统休眠或 App 退出期间不补跑错过任务。
- HTTP `401/403/429`、登录页、账号验证页、持续 GraphQL 错误、连续出现错误页面、页面结构无法确认、内容或游标循环时立即熔断。熔断后不自动重试验证、不切换代理、不清 Cookie；保存断点并要求用户处理。
- 风控参数是降低不必要访问的工程约束，不作为“模拟真人”或规避平台检测的保证。界面应明确提示使用私有接口和自动化可能违反平台规则或触发账号限制。

### 6. 双通道解析

书签和推文采用“页面响应优先、DOM 核验补充”：

- 监听由书签页自然加载产生的 Bookmarks 响应，不主动重放 Bookmarks GraphQL，不从客户端脚本提取 queryId 后自行分页。
- 从响应提取 tweet ID、作者、时间、普通正文、Note Tweet、引用对象、媒体、展开链接和 Article ID。只消费页面已经发出的响应，不修改请求参数。
- DOM 用于识别加载结束、登录/验证/错误状态以及滚动容器，不把无障碍文本或整个页面 `innerText` 直接当作正文。
- 每轮滚动后按 tweet ID 去重；连续多轮无新 ID 且底部状态稳定才判定当前批结束。无法确认是否到底时标记断点，不声称全量完成。

Article 采用“结构化响应 + 渲染 DOM 累积”的双通道：

- 优先解析详情页自然产生的 Article 结构化响应；同时滚动详情页，对当前可见正文块做稳定标识和顺序累积，以应对懒加载或虚拟列表回收。
- 不只保存某一时刻的 DOM。每次滚动记录块类型、文本、标记、链接、媒体引用和相邻顺序，按稳定键去重；滚动到底后再次向上核验首尾与块数。
- 表格优先保存二维单元格 JSON，并生成可读 Markdown；跨行、跨列或复杂内容无法无损转换时，Markdown 给出可读版本，结构化 JSON 保留原单元格关系并标记局部降级。
- 内嵌推文只保存详情页响应或 DOM 已提供的内容与原链接，不额外打开其详情页。图片保存说明、顺序、原始 URL 和本地化状态。

### 7. 完整性判定

Article 只有同时满足以下条件才标记 `complete`：

- 标题存在，正文不是预览或只有 t.co 链接。
- 已到达 Article 页面底部，连续两次采集没有新增正文块。
- 正文块顺序稳定，首尾核验一致，不存在未解析的正文占位块。
- 列表、引用、代码块、表格、链接和内嵌对象均已解析或明确记录局部降级原因。
- 所有图片引用均已发现并保存原始 URL；图片本地下载失败不影响正文 `complete`，但附件状态单独标记。
- 视频不参与 Article 完整性要求，只保留封面、标记和原链接。

完整性不再只使用 `partial: boolean`，改为：

- `contentStatus`: `complete | partial | failed`
- `attachmentStatus`: `complete | remote-only | partial | none`
- `captureMethod`: `cdp-response | cdp-dom | cdp-hybrid`
- `partialReasons`: 结构化原因数组，例如 `article-not-at-bottom`、`unknown-block`、`table-degraded`、`image-failed`、`login-required`
- `capturedAt`、`lastVerifiedAt`、`sourceUpdatedAt`、`schemaVersion`

### 8. 存储与用户编辑

Markdown 继续作为权威可读正文，同时为每条 X 记忆保存结构化 JSON。原始整页 HTML 不作为默认产物，避免页面噪声、敏感会话内容和体积膨胀；调试快照必须脱敏且仅在用户显式开启后临时保存。

每份记忆明确拆分自动原文与用户笔记：

```markdown
---
source: https://x.com/author/status/id
contentStatus: complete
attachmentStatus: remote-only
captureMethod: cdp-hybrid
---

# X 原文

由采集器维护，可在取得更完整内容时更新。

# 我的笔记

由用户维护，采集器永不覆盖。
```

- 再次获取到更完整原文时，仅更新“X 原文”和结构化 JSON；“我的笔记”永不被采集器覆盖。
- 原文更新前保留上一次结构化快照摘要与内容哈希，写入采用临时文件加原子替换。出现本地并发修改时保留双方并提示合并。
- JSON 保存块级类型、顺序、标记、表格单元格、媒体、链接、引用关系、采集状态和错误原因，不保存 Cookie、授权头或完整网络响应。
- 图片正文先引用原始 URL；本地化成功后替换或增加相对路径，同时保留原始 URL 作为来源元数据。失败不阻塞正文归档。
- 本地归档永久保留。用户在 X 取消收藏不会自动删除、移入回收站或触发全量比对。

### 9. 模块职责

- `ChromeProfileManager`：发现 Chrome、管理专属 Profile、受管进程、环回调试端点和窗口显示位置。
- `CdpSession`：通过 `connectOverCDP()` 连接和恢复页面，监听页面状态与网络响应，不保存认证数据。
- `BookmarkScroller`：控制书签页滚动、去重、20 条已知截断、每日额度和断点。
- `BookmarkResponseParser`：将页面自然产生的响应转成统一 Tweet/Article 发现记录。
- `ArticleCollector`：串行打开 Article，融合响应与滚动 DOM，输出块级结构及完整性证据。
- `ArchiveCoordinator`：将发现、Article、图片队列解耦，负责幂等写入、暂停、恢复和熔断。
- `MediaResolver`：记录远程 URL，低并发本地化图片；跳过视频流。
- `RiskGuard`：持久化每日额度、冷却、错误计数和熔断原因，禁止采集模块自行无限重试。

### 10. 迁移与兼容

- 旧 Markdown 在首次读取时映射为 `contentStatus: partial | complete`，不立即批量重写；再次采集或用户保存时写入新字段。
- 已有 `partial`、`capturedImages` 和 checkpoint 暂时保留读取兼容，新代码写入新版状态；迁移完成前不得删除旧字段。
- CDP 断点按账号 ID、Profile ID、扫描方向和最近 tweet ID 保存，不依赖 GraphQL cursor 长期有效。
- 旧的 Electron Session、加密 Cookie 和 Bookmarks endpoint 不自动复制到 Chrome；用户需在专属 Chrome Profile 登录一次。
- 同步只包含 Markdown、结构化 JSON 和本地图片，不包含 Chrome Profile、调试端点、浏览器日志和风控状态中的敏感细节。

### 11. 异常与边界

- Chrome 缺失、版本不受支持、Profile 被占用、调试端点不可达、页面被用户关闭、网络/代理失败分别提示，不混称 Cookie 失效。
- 页面在屏幕外时若 Chromium 降低渲染或暂停加载，采集器停止并提示显示窗口；不切换无头模式绕过。
- X 页面结构或响应 schema 变化时保存未知样本的脱敏摘要，当前条目标记 `partial`，达到阈值后整体熔断。
- Article 删除、仅关注者可见、订阅限制、地区限制或账号无权访问时记录明确原因，不用预览冒充完整正文。
- 图片远程 URL 过期、重定向到非允许域名、文件超限或格式不支持时保留原链接与失败原因。
- App 或 Chrome 崩溃后从最后已原子保存的 tweet ID 和 Article 队列恢复，不重复覆盖用户笔记。

### 12. 阶段任务

1. 将 v0.1.0 登录与采集抽象为可替换适配器，补齐新版状态模型及向后兼容测试。
2. 实现 Chrome Profile 与 CDP 生命周期，只验证首次登录、重启复用、环回端点和异常退出恢复。
3. 实现书签页响应监听、自动滚动、去重、20 条已知截断、每日额度和断点，不主动重放 GraphQL。
4. 实现普通推文、Note Tweet、引用推文解析和 Markdown/JSON 双写。
5. 实现 Article 详情页双通道采集、虚拟滚动累积、表格结构、完整性判定及视频跳过。
6. 解耦图片本地化队列，完成远程 URL 回退、限并发和失败重试。
7. 接入启动后自动扫描、暂停/显示 Chrome/熔断 UI，迁移旧数据并执行真实账号小批量验收。
8. 先以 20 条、再以 100 条、最后按每日 200 条逐级放量；任一级出现验证或限流即停止升级。

### 13. 测试与验收

- 单元测试覆盖响应 schema 变体、普通推文、Note Tweet、引用推文、Article 块、复杂表格、去重、完整性状态、每日额度和熔断。
- 使用本地合成页面验证 CDP 连接、滚动、虚拟列表回收、页面关闭、窗口移出屏幕、网络失败和崩溃恢复，不用真实账号做高频自动化测试。
- 真实账号验收第一阶段只扫描 20 条，人工抽查 5 条普通推文、3 条 Note Tweet、3 篇 Article、1 个复杂表格和图片远程回退。
- 第二阶段扫描 100 条，验证重复启动、暂停恢复、连续 20 条已有截断、用户笔记不覆盖和图片延迟补齐。
- 前两阶段均无验证、限流和明显缺失后，才启用每日最多 200 条历史回溯。500–2,000 条分多天完成，不以单次跑完作为验收标准。
- 验收 Article 时同时核对标题、首段、末段、总块数、表格行列、链接和图片数量；视频只核对封面、标记和原链接。
- 实际功能测试完成前不得把 v0.2.0 标为已实现，不更新 ChangeLog 为完成，不提交发布版本。

### 14. 验收标准

- 重启 XMemeory 后可复用专属 Chrome Profile 登录态，不读取日常 Chrome Cookie，也不在 XMemeory secrets 中新增裸 X Cookie。
- 采集只消费书签页和 Article 页自然产生的请求；测试与日志能证明未主动调用 Bookmarks GraphQL 分页。
- 历史回溯按日额度停止并可恢复；增量连续命中 20 条已有书签后停止，无无限滚动或重复循环。
- 普通推文、Note Tweet、引用推文和 X Article 能写入 Markdown 与结构化 JSON；复杂表格保留二维结构。
- Article 完整性有可核验依据，无法确认时必须为 `partial`，不能用预览文本标记为完整。
- 视频不下载；图片下载失败时原始 URL 仍可用，正文归档不被阻塞。
- 原文补全不会覆盖“我的笔记”；旧资料可读取，迁移失败不破坏原文件。
- 登录/验证/限流/页面异常触发熔断并保留断点，不自动绕过或持续重试。

## v0.1.0 方案变更 — 应用内登录窗口记住登录态（2026-09-15）

状态：用户反馈 Playwright 拉起的浏览器登录无响应，怀疑未走系统代理；并询问是否改为读取 Chrome Cookie。

### 结论

- Playwright 新开的 Chrome/Edge **不会自动使用系统代理**，在本机已开代理时登录页会转圈或无响应；自动化特征也可能让 X/Google 登录卡住。
- **直接读取日常 Chrome Cookie 数据库在 Windows 上不可靠**：Chrome 127+ 使用应用绑定加密，外部进程不能稳定解密，也不能在 Chrome 正在运行时安全打开其配置目录。
- 采用 **Electron 持久化分区 `persist:xmemeory-x`**：登录窗口与采集请求共用同一 Chromium 会话，显式 `setProxy({ mode: 'system' })`，与此前能返回 HTTP 200 的路径一致。登录 Cookie 留在应用数据目录，重启仍在。

### 范围

- 「登录 X」打开应用内窗口，加载书签页，走系统代理；检测到 `auth_token` 与 `ct0` 后保存并关闭。
- 拦截窗口内真实 Bookmarks GraphQL 以记录 queryId。
- 采集使用同一持久化会话（含 Cookie 与系统代理）。「退出登录」清除该分区。
- 插件导出 Cookie 仍为备用，导入后写入同一分区。
- 不读取用户日常 Chrome 配置，不把登录态写入资料库或 GitHub。

### 限制

- Google/Apple 网页授权可能拒绝内嵌窗口；请优先用 X 用户名和密码（及短信/App 二次验证）。
- 换设备需重新登录。

## v0.1.0 记忆验收进度 — 本地归档与阅读（2026-09-14）

用户已确认灵感验收完成，并要求开始记录/记忆功能。本轮在既定 v0.1.0 范围内落地可离线阅读的书签归档：合成 GraphQL 分页、断点游标、用户改稿不被覆盖、视频只留封面、失败图片进入补抓队列。界面提供采集进度、作者/媒体筛选、列表与卡片、打开原文和复制链接。真实 X 登录与全量历史完整性仍单独验收，不把合成测试当作真实采集完成。

关闭流程补充（2026-09-14）：以实际正文修改状态决定是否请求保存，无修改直接关闭；保存失败与确认超时分别呈现，保存响应关联请求 ID。保留既定冲突恢复副本机制，验证无编辑退出不误报。属于异常处理修复，不改变业务范围。

## v0.1.0 记忆验收进度 — X 网络连接修复（2026-09-14）

用户已确认灵感验收完成，当前转入既定记忆/书签记录功能，优先解除导入 Cookie 后 `fetch failed` 的阻断。

实测系统启用代理：Node fetch 访问公开 X 首页连接超时，Electron Chromium 网络栈访问返回 HTTP 200。X 采集请求改用独立、不持久化的 Electron session，使用系统代理/PAC，保留显式导入 Cookie、HTTPS 主机白名单、超时与重定向限制，不读取浏览器登录数据库、不绕过证书验证。

错误按 DNS、代理、连接超时与证书问题显示；公开首页和客户端配置分别显示请求阶段并检查 HTTP 状态。仅修复 X 网络路径，不扩大到模型/同步网络改造。公开连接成功不等于真实 Cookie 登录、书签采集完整性已通过；真实账号验收继续单独记录。

## v0.1.0 补充设计 — 灵感周历与月历（2026-09-14）

状态：核心交互已确认。用户明确默认按周展示，可选择某个整月，日期格可快速创建标题；一个灵感就是一条 Todo，不区分两类条目。已使用灵感加删除线，未使用条目留在原日期，不算逾期、不催办。划线完成按保留记录实现，真正删除沿用独立回收站操作。现有 v0.1.0 已确认范围保持有效。

### 目标与范围

实施记录：2026-09-14 已实现周/月切换、日期格标题创建、勾选划线、Markdown 日期/完成状态及回读、插入链接和创作素材反馈。核心测试、真实 Electron 日历专项与既有编辑/退出恢复回归已执行，结果见 ChangeLog；当前交付为本地验收包，不代表完整首版发布。

优先完成灵感的桌面验收闭环。本次新增周历和月历，修正链接输入与加入创作素材的反馈，不推进 X、模型或同步的新功能。灵感本身就是可勾选的一条 Todo，可带 Markdown 正文、图片和链接；不新增独立 Todo 类型、逾期状态、催办、自动顺延、提醒或重复任务。

### 使用流程与默认交互

1. 点击侧栏“灵感”默认进入本周，周一至周日；提供上一周、下一周、回到本周。可切换月视图，通过年/月选择器展示任意完整月份，提供上月、下月、回到本月。周/月切换保持当前选中日期。周视图每天展示标题列表和完成状态，点击条目打开宽编辑区；月视图展示标题与数量。
2. 每个日期支持快速输入标题，Enter 创建一条灵感并保存，空标题不创建；只有标题也算完整条目，不强制填写正文。新建条目归属所选日期，支持过去和未来日期，不再出现“新建 Todo”或类型选择。
3. 点击标题进入编辑器，可返回日历并保持当前周/月与选中日期。正文沿用 Markdown、图片、表格和双链。标题旁复选框只切换完成状态，不意外打开编辑器。
4. 勾选表示灵感已使用，标题加删除线、视觉弱化并继续留在当天；可取消勾选恢复。未使用条目始终留在原日期，不提示逾期、不自动迁移、不催办。加入素材或生成候选稿不自动标完成。真正删除使用原有删除/回收站流程，完成不等于删除文件。
5. “加入素材”明确改名为“加入创作素材”。点击先保存，再加入当前创作的素材列表，显示“已加入创作素材”和数量，并可打开素材面板查看/移除；不自动调用模型，不等于保存图片，也不等于灵感已使用。避免重复添加。首版素材选择仍限本次 App 会话，界面需说明。
6. Markdown 标准链接 `[tes](https://www.baidu.com)` 应正确渲染。`[tes](baidu)` 中 baidu 为相对目标，不猜测其网址；反斜杠转义的 `[tes]\\(baidu)` 若包含实际转义符则属于字面文本。新增显式“插入链接”入口，支持链接文字与地址；完整域名可补 https，缺少域名后缀提示填写完整地址，不破坏本地笔记相对链接。富文本手动输入标准链接在完成语法后应识别；源码模式切换必须得到同样结果。

### 数据、接口与迁移

- Note 新增可选字段 `scheduledDate: YYYY-MM-DD`、`completed: boolean`、`completedAt`；沿用 inspiration 类型与 inspirations/，不新增 entryType 或独立 Todo 类型。
- 日期是用户选择的本地日历日期，不因时区切换自动移动。创建时间与日历归属日期分别保留；完成状态和日期写入 Markdown Front Matter，使外部编辑回读和现有同步能保留。
- 缺少新字段的旧条目视作灵感、未完成，按 createdAt 的本地日期显示；无效日期回退到文件日期并提示。只在实际编辑时写入新字段，不批量重写旧文件。
- 保存 IPC 对新字段做严格校验；全局搜索包含所有灵感及其完成状态，已完成仍可检索、打开和加入创作素材。
- 原资料库格式保持兼容；旧版不识别新字段，避免同时运行新旧版修改状态。

### 异常处理与验收

- 保存失败不切换日期或编辑目标；沿用冲突保留双方和退出恢复副本机制。
- 周历/月历覆盖跨年周、闰年二月、月初/月末、跨月日期、周/月切换、按天快速创建标题、空标题校验、新建过去/未来日期、划线完成与撤销、重启状态保留；跨天后未完成条目仍留在原日期，不能出现逾期提示或自动顺延。
- 外部修改日期/完成状态后，月历、日列表与搜索一致更新；同名条目按 ID 区分，删除/恢复不丢归属日期。
- 验证源码/手动输入/粘贴三条路径的标准网页链接、本地链接、表格内链接；字面转义不被错误改写为网页链接。
- 加入素材可见反馈、数量去重、查看移除、切换笔记仍保留选择；不产生模型请求，不自动完成灵感。
- 在真实 Electron 窗口完成以上行为测试，更新 ChangeLog 后交付新的本地验收包。无新外部服务依赖；签名和跨平台限制沿用原计划。

## v0.1.0 — 首个可用版本

- 日期：2026-09-13。
- 状态：已确认，开发中。用户于 2026-09-13 明确要求“开始开发”，确认当前方案及所列首版默认边界；真实平台验证、账号接入和发布按实际完成情况记录。
- 确认记录：用户已确认名称为 XMemeory、Electron 路线、正文优先、不保存评论赞、限速、仅保存被收藏的单条正文、Cookie 无法直接读取时本地导入；已选择“插件手动导出、应用自动导入”。核查模型能力后，用户决定首版只配置 DeepSeek、暂缓联网；书签少于 1,000 条；正文必须直接保存 Markdown，并支持外部编辑回读。用户随后明确要求“开始开发”和“继续开发”，已授权本方案范围内的本地实现；强调交付为桌面 App。
- 规划前项目只有 `AGENTS.md`，没有已有实现、依赖、测试或 Git 仓库；本次新增本文档。
- 产品名称严格使用用户指定的 XMemeory（保留此拼写），同步更新项目规则中的产品名和 README 标题要求；当前目录名 XMemory 保持原样，不移动目录。
- 使用场景与平台确认：以短推和串文为主；Windows 10 及以上，Mac 同时支持 Intel（x64）与 Apple Silicon（arm64）。用户表示代码仓库后续提供，不因仓库地址未定反复追问；实际推送和发布仍需目标明确。
- 最新确认补充：首版支持表格、嵌入、双链；Agent 可读取全部记忆；使用个人独立 GitHub private repo，冲突保留双方交由使用者处理。用户进一步明确必须有面向使用者的全局搜索，可查找历史所有已保存信息和候选稿，覆盖本资料库全部灵感、记忆、已保存创作记录和候选版本，不扩展到 X 浏览历史或其他应用数据。
- 草案修订记录：第一轮提出本地检索和多种登录备选；第二轮研究模型原生联网和 Cookie 自动导入；用户随后选择暂缓联网，并要求 Markdown 外部编辑回读。本轮以这些最新决定为准，联网研究仅保留为后续依据；原 JSON 正文方案被 Markdown 权威正文方案替代。

### 1. 背景与目标

做一个本地优先的个人 X 创作桌面应用，将“记录灵感 → 保存和检索书签 → 用素材生成推文”连成可持续使用的流程。Mac 与 Windows 使用相同的数据格式，并通过用户指定的 GitHub 私有仓库同步。

首版交付目标是：图片粘贴后重启仍可查看；可访问的历史书签能持续归档并补抓新增内容；创作结果可以追溯引用的记忆；另一台设备能够恢复文本与图片，发生冲突时不丢稿。

“全量保存”指当前账号在 X 实际返回、且拥有访问权限的书签内容。平台不返回的历史内容、已删除内容和失去权限的内容必须明确标记，不能以“已获取所有历史”代替实际完成情况。

### 2. 功能范围与非目标

| 模块 | v0.1.0 范围 |
| --- | --- |
| 灵感 | 多篇灵感、新建/编辑/搜索/删除恢复、Markdown 兼容富文本、表格、双链、图片与笔记嵌入、粘贴和拖入图片、Markdown 自动保存、外部编辑回读、图文导出 |
| 记忆 | 单个 X 账号的书签归档、正文与媒体保存、历史分页、断点续传、增量扫描、离线阅读、全文检索、来源跳转、失败重试 |
| 创作 | 选择灵感和记忆，DeepSeek 检索本地素材，流式生成单条推文或串文，追问修改、版本保留、复制结果；联网暂缓 |
| 全局搜索 | 用户直接搜索全部已保存灵感、记忆、创作记录及候选稿/候选历史版本，支持类型、日期、作者、标签筛选，命中高亮和原文定位，离线可用 |
| 模型配置 | 首接 DeepSeek；可配置服务名称、Base URL、API Key、模型；首版允许增加 OpenAI 兼容服务配置 |
| 数据同步 | 用户指定的 GitHub 私有数据仓库，文本和图片双向同步，冲突副本、同步状态与重试 |
| 桌面体验 | Mac 与 Windows；浅色、深色、跟随系统；系统托盘/菜单栏驻留及快捷键 |

范围解释（已明确的正文与限速要求，以及待确认的其他边界）：

- `https://x.com/i/history` 作为用户给定入口；采集目标暂定其中的书签集合，不默认包含点赞、观看记录或一般浏览历史。开发时在真实登录页面核实标签与实际数据来源。
- 用户已明确不保存评论、点赞等互动数据；不采集评论正文、评论列表、点赞列表及点赞/评论/转发计数。归档集中于正文、封面、图片和链接；不为互动信息增加请求。
- 视频和动态媒体不下载视频流、不转码、不转写；保留可取得的正文、封面、独立图片、外链及原文链接。GIF 若由视频提供，按视频处理。
- 保存被收藏的单条推文本身完整可取得正文，包括该条长推正文；不追抓作者后续串文或其他回复。X Article 若是收藏对象所承载的正文则补取，若只是站外/相关文章链接则只保存卡片和链接；真实对象结构在阶段 A 核实。
- 引用卡片保存源端已随当前条目返回的文字、图像与链接，不额外遍历引用对象。外部网页首版保存链接及 X 已返回的卡片信息、封面，不递归抓取站外全文。
- 用户最新决定：首版只配置 DeepSeek，暂缓联网检索；后续若增加联网，优先模型服务商原生 Web Search，不默认引入独立搜索 API。当前只做本地资料检索。
- 图片首版用于保存、查看和选作配图；默认仅把文本送给模型。模型看图/OCR 不列为首版必达项。

非目标：自动发推、定时发推、视频下载、多账号管理、多人协作、移动端、向量数据库、自动训练个人模型、任意工具插件市场、云端常驻采集、端到端加密同步和自动更新安装器。

### 3. 主要使用流程与界面

#### 3.1 首次启动

1. 选择本地资料库位置；展示数据占用与当前路径，并提供在文件管理器打开的入口。
2. 进入灵感即可离线记录；X、模型、GitHub 独立配置，不要求全部完成才能使用。
3. 在“记忆 → 连接 X”选择登录态来源，验证并显示账号名称和账号 ID 后开始归档。
4. 在设置中添加 DeepSeek 配置并测试连接；填写模型名称或读取服务可用模型列表。
5. 可选连接 GitHub 私有数据仓库，预览将同步的内容范围后开启同步。每台设备独立配置凭据。

#### 3.2 灵感编辑

左侧导航为灵感、记忆、创作；灵感中间为条目列表，右侧为编辑区。支持标题、段落、列表、加粗、引用、链接和图片；避免首版变成复杂排版软件。

粘贴截图或图片 → 就地显示待保存占位 → 校验和写入本地媒体库 → 替换为稳定的资源引用 → 显示“已保存”。失败保留正文及重试入口。支持多图顺序、撤销/重做、图片删除和重启恢复。

输入采用约 500ms 防抖保存；切换条目和退出时刷盘，尚未保存成功时提示。内容为空、中文输入法组合输入、粘贴图文混合 HTML 均需覆盖。

#### 3.3 记忆归档和阅读

点击首次归档后，列表不断出现已保存条目。状态区域区分“正在回溯历史”“扫描新增”“下载附件”“需要重新登录”“部分失败”。展示发现数、正文已保存数、附件已完成数、失败数、最近成功时间和当前保存位置；总量未知时不显示虚构百分比。

记忆支持关键词、作者、标签、媒体类型筛选，列表/卡片切换，以及正文详情、原图查看、链接复制和在浏览器打开原文。每条记忆显示正文完整性和附件状态；未能取得的内容给出原因。

默认每 30 分钟扫描，可设 15/30/60/180 分钟或关闭；有“立即扫描”“暂停”“继续”“重试失败项”。关闭窗口默认驻留，可设置为直接退出。完全退出、关机和睡眠期间不运行；恢复后补一次到期扫描，不补跑全部错过的定时任务。

#### 3.4 创作

从灵感点击“用于创作” → 输入目标、语气、语言、单条或串文 → 固定选中的素材并允许 Agent 搜索授权范围内的本地记忆 → 展示命中材料与可发送范围 → DeepSeek 生成 → 编辑或要求改写 → 保存候选版本 → 复制到 X。首版不展示可用的联网开关。

界面展示“检索素材 / 生成中 / 已停止”等动作进度，不要求显示模型内部思维。结果旁保留来源清单，可点回本地记忆和原文；引用关系不强制插入最终推文正文。

默认目标为 280 加权字符，允许修改创作长度目标；按 X 公开规则支持的实现计算加权长度和 URL，不用 JavaScript 字符串长度冒充 X 计数。串文逐条计数并允许手动调整。粘贴到 X 后仍由 X 作最终发布校验。

用户已确认首版以短推和串文为主：创作入口提供“短推 / 串文”模式，串文以有序条目展示，支持逐条编辑、增删、调整顺序、逐条复制和整组复制。每条显示独立字数与超限提示，编号计入输出字数；不自动发布。专用长文创作模板不列为首版必达项，但归档正文和灵感内容不因短推输出目标被截断。候选版本保存完整串文顺序，全局搜索命中后能定位到相应条目和版本。

#### 3.5 视觉规范

采用 Mac 风格的克制布局：窄侧栏、清晰层次、充足留白、细边框、适度圆角，低饱和蓝色作为单一强调色。正文阅读宽度约 680–760px，主要文字 14–16px；窗口较小时可收起侧栏。

- 浅色：暖白内容区与浅灰侧栏；深色：炭黑内容区，避免纯黑大底导致层次消失。
- 主题选项为浅色、深色、跟随系统，选择即时生效并持久化，默认跟随系统。
- 使用系统字体栈，Mac 优先系统字体，Windows 使用系统等价字体，不捆绑 Apple 字体。
- 标题栏、窗口按钮遵循所在平台；快捷键在 Mac 使用 Cmd，Windows 使用 Ctrl。
- 保证键盘焦点可见、状态不只靠颜色区分、文本对比度至少 4.5:1；加载、空列表、离线和错误均有明确界面。

#### 3.6 全局搜索（用户直接使用）

全局搜索是首版独立入口，不依赖创作 Agent 或模型配置。侧栏提供“搜索”，Mac 使用 Cmd+K、Windows 使用 Ctrl+K 打开；在任意模块输入关键词即可跨库查找，默认不限内容类型和日期。

- 搜索范围：所有已保存灵感、历史书签记忆、创作要求及已保存可见创作记录、候选稿及其保留版本。这里的“所有”指应用资料库中的已保存内容，不读取其他应用、浏览器历史或任意磁盘目录；未保存输入不作为已归档结果。
- 搜索字段：标题、正文、表格文字、标签、作者名称/用户名、来源链接、双链显示名及目标名称、已有图片说明/文件名。图片像素内文字、视频音轨不做 OCR/转写搜索；不能把附件已保存等同于图内文字已可检索。
- 支持中文、英文、混合关键词、多词组合和引号短语；查询作为普通文本解析，转义全文检索特殊字符，避免语法字符导致查询错误。首版以词语匹配为主，不承诺纯同义词语义召回。
- 筛选包括内容类型、标签、记忆作者、保存/修改日期，候选稿可筛选“全部保留版本/当前版本”。默认搜索全部保留候选版本，同一创作的多个命中折叠分组，展开后显示版本时间和状态。
- 结果展示类型、标题、命中摘要、关键词高亮、日期、候选版本标识及原文来源；按相关度或时间排序，分页加载。点击直接进入对应条目和命中段落；旧候选稿打开对应旧版本，不跳到当前稿。
- 支持将结果“加入本次创作素材”；此操作才将选定材料纳入当前创作范围。普通搜索、预览和排序完全在本机执行，不调用 DeepSeek、不消耗模型额度、不上传查询或命中内容。
- 未命中时保留查询和筛选，提供清除筛选入口；索引尚未完成或损坏时显示状态，不能把索引不可用伪装为没有数据。
- 回收站和冲突副本默认从普通结果排除，可显式开启“包含回收站/冲突副本”检索并标注状态。已彻底清理的内容不承诺恢复；后台操作日志、Cookie 和密钥不参与索引。
- 新保存、候选版本新增、外部 Markdown 回读、GitHub 同步、改名、删除恢复后增量更新索引；正常 1,000 条规模下目标为保存成功或外部文件稳定后的 2 秒内可检索。索引损坏可从持久化内容重建，重建不修改源文件。

### 4. 技术方案与模块职责

Electron 路线已确认；建议 React + TypeScript + Vite，编辑器使用 Tiptap，模型适配优先采用 Vercel AI SDK 的明确 provider 实例；依赖管理使用 pnpm 并维护锁文件。版本在开发启动时核对稳定发布版和兼容后锁定，不直接依赖研究时的 main 分支。

选择理由：桌面剪贴板、托盘、网络会话和文件操作是首版核心；Electron 提供统一 Chromium 环境，降低双端编辑器差异。代价是安装体积和内存高于 Tauri；本阶段优先完成登录态、编辑和同步闭环。

| 模块 | 职责 |
| --- | --- |
| renderer | React 页面、Tiptap 编辑器、阅读和创作 UI；不持有密钥，不直接操作文件系统 |
| preload / IPC | 仅暴露明确的方法；校验参数、发送方与结果结构 |
| library | 内容版本、资源存储、导入导出、原子写入及 SQLite 索引 |
| x-source | 登录态适配、账号校验、分页、详情补取和统一数据映射 |
| archive-worker | 历史与增量任务、附件下载、限流、失败队列、恢复 |
| search | 跨类型本地全文索引、全局搜索、中文检索、版本分组、筛选、高亮和定位；同一索引服务为 Agent 提供受范围限制的检索 |
| agent / providers | 有限工具循环、上下文预算、流式生成、取消、服务适配 |
| sync | 私有仓库校验、快照交换、三方合并、冲突和恢复 |
| desktop | 系统主题、托盘、路径选择、凭据加密、生命周期及打包 |

Electron 设置 `contextIsolation: true`、关闭 `nodeIntegration`、启用 renderer sandbox；远程 X 页面使用隔离会话和受限导航，不加载带本地权限的 preload。正文 HTML 经清理后渲染，禁止脚本、任意 iframe 和事件属性。

正式应用数据位于用户选定的资料库；开发过程中临时脚本和日志遵循工作区规则存放在上级 `trash/` 任务子目录。自动化测试只用合成或脱敏材料。

### 5. 登录态与 X 采集

#### 5.1 接入顺序与可行性门槛

用户目标是复用本地 X 登录态。首阶段分别在 Mac/Windows 验证 Chrome/Edge 的受支持读取途径，只读取用户选定 profile 中 X 所需 Cookie，不扫描其他站点，不显示原值。

Windows Chrome 的应用绑定加密意味着不能把“直接读取浏览器 Cookie 数据库”视为双端保证。不得要求关闭浏览器安全保护、提升到系统权限或绕过加密。

首版已确认的可靠路径：用户用 Get cookies.txt LOCALLY 导出当前 X 站点 Cookie → XMemeory 自动导入；同时保留文件选择/拖入作为兜底。直接读取浏览器数据库不是硬性验收条件；受支持的自动读取可以在阶段 A 验证后启用，失败即提示导入，不扩大排查到其他站点。应用内登录不列为已确认必做项。Cookie 不经聊天、GitHub 或模型转发。

首版浏览器和操作系统支持矩阵根据阶段 A 实测记录；插件导出的有效 Cookie 文件必须在两端可导入，不能把解析成功当成 X 登录成功。

#### 5.1.1 插件源码核查及自动导入设计

核查用户给定商店 ID 对应的 `kairi003/Get-cookies.txt-LOCALLY` 源码（commit `0e9952c`）：`popup.mjs` 在用户点击导出后读取 Cookie；`save_to_file.mjs` 调用 `chrome.downloads.download` 写文件；`background.mjs` 的 Cookie 变化事件只更新图标计数。manifest 没有 Native Messaging 权限，已查代码没有对桌面应用自动推送或周期导出实现。商店安装包未反编译核对，以上结论限定为已读源码，不宣称审计了安装包。

该插件读取浏览器提供的 Cookie API，与外部进程解密 Cookie 数据库是两条不同路径。扩展正常授权后导出文件可避开应用直接读数据库的兼容难点；仍需用户在浏览器维持有效登录。

用户选择的是“自动导入”，不是“Cookie 永久自动续期”。流程和边界：

1. 首次在设置中选择一个本机接收目录或具体文件，明确授权监听范围；推荐新建专用接收目录。若选下载目录，只匹配 X 导出文件，不读取其中其他文件，不递归扫描磁盘。
2. 插件在 X 页面使用“导出”或“另存为”，文件名通常为 `x.com_cookies.txt` / `.json`；兼容浏览器生成的 `x.com_cookies (1).txt` 等编号副本。另存为的目标位置由用户选择，不能假设插件能记住应用要求的任意绝对路径。
3. 监听文件完成写入/重命名，忽略 `.crdownload` 等未完成文件，等待大小和修改时间稳定后再读；去抖、内容散列去重，支持启动时检测授权目录中未处理的候选文件。
4. 解析 Netscape 七列格式和插件 JSON 数组；支持 CRLF/LF、BOM、会话 expiry=0、`#HttpOnly_` 前缀。Header 格式缺失 domain/path 信息，首版不自动导入此格式，提示重新选择 Netscape 或 JSON。
5. 严格限制输入大小（拟 1 MiB），拒绝符号链接/路径逃逸，只保留 X 必要域和字段，不保留 Export All 中其他站点的内容。按 domain + path + name 区分 Cookie，不单按 name 合并；缺失元数据不凭空恢复。
6. 新文件作为候选凭据验证账号，验证请求也走 X 限速队列。同一账号验证成功后原子替换本机加密凭据并继续任务；账号变化要求选择，失效/损坏候选不得覆盖现有有效会话。
7. UI 只显示账号、导入时间和状态，不显示 Cookie 值。监听目录、处理记录和凭据都不进入数据同步。文件删除、移动、权限失效或事件丢失时可手动重新选择/导入。
8. 原导出文件属于用户内容，默认不擅自删除；提供“导入成功后清理接收文件”的单独可选设置。文件含明文登录凭据，接收目录应在 GitHub/其他云同步范围之外。
9. 暂停监听和断开 X 连接是两个独立动作；断开时停止采集并清理应用持有的登录凭据，不删除已归档的正文。

若以后需要浏览器 Cookie 变化时自动推送，另做限 X 域权限的配套扩展，通过 Native Messaging 与已配对本机应用通信；需安装注册和双端维护，不纳入当前已选的首版方案，也不通过修改现有第三方插件实现。

#### 5.2 数据源

封装 `XSource`，将网页会话来源与业务归档逻辑分离。登录后识别真正的书签数据，读取分页和必要详情；网页内部接口不作为稳定公共契约，端点、字段和游标行为需要实测。

官方 X API 提供书签查询接口，但使用用户 OAuth 授权，不能把 Cookie 直接当作该接口凭据。首版按用户要求研究网页会话路径；官方 API 仅作为后续可选方案，不默认增加开发者接入或费用。

采集前先验证账号；切换账号时使用独立命名空间和 checkpoint。会话过期暂停并请求重新连接，保留全部已归档内容。

#### 5.3 保存范围和媒体规则

保存推文 ID、作者 ID/显示名/用户名、发布时间、抓取时间、完整可取得正文、规范原文 URL、外链原始与展开 URL、引用对象、封面及所有独立图片。书签添加时间仅在源提供时记录，否则为 null，不用推文发布时间代替。

资源保存原始可取得图片字节，生成本地缩略图。每个资源记录 SHA-256、MIME、尺寸、大小、原始来源地址、用途、下载状态；正文只引用相对资源 ID，不保存机器绝对路径。

正文被截断时尝试详情补取；仍不完整则标记 `partial`。已删除、受限、失效封面和下载失败分别记录原因。只要文本已保存，就可阅读和检索，不因一张图失败回滚整条。

#### 5.4 历史进度与新增扫描

1. 历史回溯从第一页开始，逐页将规范内容、附件任务写入可恢复的本地日志，成功提交后才推进历史游标。
2. 正文页完成与媒体完成分开记录；媒体失败不阻塞历史遍历，持续进入独立重试队列。
3. 保存 `accountId`、数据源版本、历史 cursor、最近成功页时间、最近发现的书签 ID 集合、完成状态及失败原因。cursor 属于设备会话，不跨设备同步。
4. 增量扫描每次从最新页开始，维护独立 cursor；不能用推文发布时间筛选新增，旧推文也可能今天被收藏。
5. 首版不采用“遇到一条旧推文就停止”。未经证实的排序规则下增量任务继续分页到源端末尾；可分批执行并保存进度。稳定排序和安全重叠窗口只有在验证后才启用优化。
6. 游标过期从头重扫，以 `accountId + postId` 幂等去重；新发现内容不中断后台历史任务，任务按页轮转，单账号请求串行。
7. 定期重新遍历用于发现遗漏和可访问内容更新；没扫描到某条不等于取消收藏或删除，远端缺失不自动删除本地归档。
8. 空页、重复 cursor、结构变化、权限拒绝分别识别；结构异常和循环分页标记受阻，只有已验证的源端终止信号才标记历史遍历结束。

限速是用户明确的首版要求，不以最大吞吐为目标。拟定保守默认值如下，阶段 A 只能依据实测收紧，不能将这些数值称为平台保证安全的阈值：

- 所有 X 列表、账号验证、单条正文详情和 Article 补取共用单账号队列，并发 1，相邻请求间隔随机 8–15 秒；重试也消耗同一预算。不为串文和引用对象额外增加请求。
- 每完成 20 次内容请求休息 2–5 分钟；每轮最多 60 次内容请求，达到预算后保存 cursor，冷却至少 15 分钟再续作。手动点击不能绕过冷却，重复扫描请求合并。
- 图片下载并发 1，启动间隔 2–4 秒；失败不立即循环补抓。已验证成功的正文与相同散列附件不重复下载。
- 网络超时 30 秒；普通临时读取失败最多重试 3 次，指数退避并加抖动。429 停止当前轮并按 `Retry-After` 与至少 30 分钟冷却中的较长值恢复，连续限流逐次加长至 24 小时；401/403、验证码或异常登录提示立即暂停，等待用户处理。
- 历史回溯、定期复查和新增扫描共用预算，按页公平调度；休眠/断网恢复只排一项补扫，不集中突发请求。设置可调得更慢或完全暂停，首版不提供激进高速模式。
- 两台设备默认仅指定一台执行自动采集，另一台通过 GitHub 获取归档；自动采集角色按设备显式配置。切换采集设备时提示先暂停旧设备；离线或用户同时启用时无法承诺跨设备全局限速，不假设 GitHub 是可靠的实时互斥锁。

上述措施减少请求压力，但不能保证绝对不触发 X 风控；不进行指纹伪装、代理轮换或验证码绕过。真正出现风控时停止采集，保留已保存内容与进度。

资源 URL 只允许 HTTP(S)，校验重定向和解析后的目标地址，阻止本机、内网、云元数据地址及路径穿越；X Cookie 仅发往必要的 X 域，不能随图片或站外链接转发。

### 6. 本地数据与接口

#### 6.1 数据组织

资料库建议使用以下相对结构：

```text
library/
  manifest.json
  inspirations/<id>.md
  memories/<accountId>/<postId>.md
  drafts/<id>.md
  metadata/<entityId>.json
  assets/<sha256>.<extension>
  tombstones/<entityId>.json
  conflicts/<entityId>/<revisionId>.md
  local/index.sqlite
  local/jobs.sqlite
  local/device-settings.json
```

Markdown 文件是唯一权威正文；Tiptap JSON 与 plainText 仅为可重建的界面/索引缓存，不能反向覆盖外部更新。SQLite 负责本机任务和可重建索引。附属 metadata JSON 保存采集来源、版本关系、资源状态及创作引用，不保存第二份可编辑正文。应用编辑经持久化操作日志登记，确认 expectedContentHash 未变后原子替换 Markdown，再提交关联元数据和索引；重启重放未完成操作。SQLite 及 WAL 不直接同步。

#### 6.1.1 Markdown 与外部编辑回读契约

- 首版支持基础 Markdown 格式，并按用户要求增加 GFM 管道表格、双链和嵌入。富文本与源码模式切换不改变正文语义；不能把这些必需语法只作为未知源码保留而宣称已支持。Tiptap 官方 Markdown 扩展仍标为 Beta，必须先通过往返测试；双链与嵌入由显式节点/解析和序列化规则实现，不假设框架已完整支持。
- 应用创建的 Markdown 使用最小 YAML Front Matter：`xmemeory_id`、`type`，可选标题和标签；来源链接同时可在正文末尾阅读。未知 Front Matter 字段保留，解析使用安全 schema，不执行 YAML 标签。不可识别的结构在源码模式处理，不静默丢弃。
- 图片使用相对 Markdown 文件的标准路径，例如 `../assets/<hash>.png`；记忆文件层级相应使用 `../../assets/`。外部添加的图片可引用资料库内相对路径，检查路径和真实文件后纳入同步；资料库外图片须显式复制导入。缺图保留原引用并提示，不删除 Markdown 图片节点。
- 监听已选资料库内的内容目录和被引用附件，忽略内部状态、临时文件与 Git 目录；文件稳定后读取，应用自身写入按 hash 去重。启动/重新获得焦点时进行有界补扫，避免只依赖 watcher 而丢事件。
- 编辑器无未保存改动时自动加载外部新正文并刷新索引；有未保存改动时保留“应用内草稿”和“外部文件”两份，暂停该文档自动保存，用户选择或合并后继续。不能用 mtime 先后决定覆盖。
- 保存前检查磁盘内容 hash；外部应用不参与本应用锁，仍存在检查与写入间的竞态。应用为覆盖前磁盘版本和待写版本保留恢复副本，并在写入后复查；不宣称任意不配合的编辑器都可提供数据库级并发保证。
- 外部改名通过稳定 ID 关联，保留新文件名；不要因改名重复导入。外部新建合法 Markdown 可作为新灵感导入；缺少 ID 时先在本机建立关联，用户首次在应用保存或纳入同步时添加 ID。重复 ID 或对象类型变更标记冲突，不自动合并。
- 外部删除先标记缺失并保留最近已知版本，避免将移动/编辑器原子替换误当删除；确认稳定缺失后提供恢复或同步删除，不立刻传播 tombstone。资料库整体不可用时不批量认定删除。
- 外部修改已采集记忆后标记 `userEdited`，X 后续重抓不得覆盖；若原文出现变化，保留为候选源版本供比较。来源 ID/原文 URL 属元数据，不因正文外部编辑失去关联。
- 表格支持新增/删除行列、表头、列对齐及单元格内的文字、链接和基础行内格式；正确处理管道符转义。首版不提供合并单元格和多段落单元格，无法表示的外部复杂表格保留源码，不做有损转换。
- 双链支持 `[[笔记]]`、`[[目录/笔记]]`、`[[笔记|显示名]]` 和 `[[笔记#标题]]`，提供输入候选、点击打开及反向链接列表。默认在同一资料库解析；重名时提示选定目标并写入明确路径，不任意选第一条。链接文本保留 Markdown 形式，索引可记录稳定目标 ID；改名后提示更新受影响链接，更新前检查磁盘版本，不覆盖外部编辑。
- “嵌入”本轮按图片和本地 Markdown 笔记定义：支持标准 Markdown 图片及 `![[图片.png]]`、`![[图片.png|宽度]]`、`![[笔记]]`、`![[笔记#标题]]`。图片可预览，笔记嵌入只读展开并可跳回源文件编辑；渲染不复制源正文进宿主文件。嵌入目标修改后刷新视图。视频播放器、PDF 内嵌、远程 iframe 和 Obsidian 插件执行不属于此定义，若用户需要另行纳入。
- 嵌入解析检测循环，最大展开深度 3；超过范围、目标缺失、同名歧义时显示占位和跳转信息。禁止越过资料库目录、通过符号链接访问外部文件或触发脚本。块 ID 引用暂不列为必达语法，原样保留。
- 图文导出需包含库内被引用的附件和笔记依赖，保留相对路径；检测循环防止无限导出。选定范围外的笔记依赖在导出清单中显式显示，不默默打包整库。
- 其他不支持的 Markdown/HTML/Obsidian 语法保留原始文本，转源码或只读预览，禁止可视化往返时删除；不承诺任意 Obsidian 插件语法都可执行或渲染。
- 应用生成文件统一 UTF-8；外部文件保留已有 BOM/换行风格，无法识别编码时提示而不自动改写。允许语义等价的排版规范化，但不能静默损失正文、链接或图片。
- 导出为 Markdown + 相对图片，维持目录关系；纯文本导出不包含图片二进制。导出不依赖数据库才可阅读。

附件先写临时文件，校验后原子重命名，再提交内容引用；对崩溃产生的孤立附件只标记，不启动即删除。工作中的副本与删除记录参与同步，回收站保留直至用户主动清理。

| 对象 | 关键字段 |
| --- | --- |
| Inspiration | id、title、markdownPath、contentHash、assetIds、tags、createdAt、updatedAt、revision、parentRevision、deviceId；editorJson/plainText 仅缓存 |
| Memory | accountId、postId、kind、author、markdownPath、contentHash、userEdited、originalUrl、links、assetIds、quotedIds、publishedAt、bookmarkedAt?、capturedAt、contentStatus、revision；text 从 Markdown 派生 |
| Asset | hash、mime、size、width、height、relativePath、sourceUrl、role、downloadStatus、lastError |
| Draft | id、markdownPath、contentHash、inspirationId、sourceRefs（ID + 版本 + 引用片段）、providerId、model、request、status、parentDraftId、revision；不重复存储 output 正文 |
| Checkpoint | accountId、sourceVersion、mode、cursor、seenBoundaryIds、lastSuccessAt、status、lastError；本机专属 |
| ProviderConfig | id、name、protocol、baseUrl、model、secretRef、capabilities（tools/stream/nativeWebSearch/citations）、timeout；secretRef 仅本机，联网能力按服务端协议分别适配 |
| SyncState | repositoryId、branch、baseCommit、lastSuccessAt、pendingCount、conflicts；本机专属 |
| Tombstone | entityType、entityId、revision、parentRevision、deletedAt、deviceId |
| LinkIndex（可重建） | sourceId、rawTarget、resolvedTargetId?、heading?、kind（link/embed/image）、resolutionStatus；反向链接从该索引派生 |
| Conflict | id、entityId、baseRevision、variantRevisions、variantPaths、deviceLabels、status、resolutionRevision?；含相关资源引用，随数据同步 |
| SearchDocument（可重建） | entityId、entityType、versionId、title、plainText、tags、author、sourceUrl、savedAt、updatedAt、status、contentHash；按实体和版本唯一索引 |
| SearchHit | entityId、entityType、versionId、score、snippet、matchRanges、sourceAnchor、contentHash；高亮用安全文本节点，不拼接外部 HTML |

时间统一 UTC ISO 8601，显示使用本机时区；X ID 使用字符串，资源名使用散列，规避数值精度和双端文件名差异。`schemaVersion` 和应用版本分开管理。

#### 6.2 应用边界接口（设计契约）

| 接口 | 输入 / 输出 |
| --- | --- |
| inspiration.save | id、expectedRevision、expectedContentHash、markdown → revision、contentHash、savedAt；版本或磁盘内容不匹配返回冲突 |
| asset.import | clipboard/file handle → assetId、mime、尺寸；失败返回可重试错误 |
| x.connect | sourceKind、选定 profile 或本地导入句柄 → accountSummary、connectionStatus |
| archive.start / pause / resume | accountId、mode → jobId；进度通过事件通知 |
| memory.search | query、filters、cursor、limit → items、nextCursor |
| library.search | query、type/date/author/tag/statusFilters、versionScope、sort、cursor、limit → SearchHit[]、nextCursor、indexStatus；用户界面可查全部已保存内容 |
| library.openSearchHit | entityId、versionId、sourceAnchor、contentHash → 对应文档及位置；内容已变则重新定位并提示 |
| memory.read | accountId、postId → 完整本地内容、来源与附件状态 |
| agent.run / cancel | inspirationId、sourceIds、instruction、providerId → runId 和事件流 |
| sync.preview / run | repositoryRef → changeSummary / jobId、conflicts |
| sync.resolveConflict | conflictId、expectedVariantRevisions、action、mergedMarkdown? → resolutionRevision / 新冲突；保存前重新检查参与版本 |

所有参数做 schema 校验；错误返回稳定 code、可读 message、retryable，不返回 Cookie、请求头、密钥或完整私有响应。运行任务通过 jobId 关联，重复点击不会启动重复任务。

### 7. 创作 Agent 与模型扩展

首个适配器使用 DeepSeek 的 OpenAI 兼容接口，默认 Base URL 为 `https://api.deepseek.com`。模型 ID 从配置读取，部署前对照官方文档和连接测试，不硬编码旧模型名为永久默认。

首版提供 `ProviderAdapter`：模型发现（可选）、连接测试、能力声明、流式生成、错误转换。兼容服务可新增配置；不兼容协议需要后续适配器，不能承诺任意 API 仅改 Base URL 就能工作。请求路径统一规范化，避免重复 `/v1`。

Agent 的本地工具仅提供 `search_memories`、`read_memory`、`read_inspiration`，参数校验并限制结果数量。流程为本地召回 → 合并候选和来源 → 生成；本地工具最多 4 轮，超预算明确提示。模型不支持本地工具调用时降级为固定检索后生成。

用户已允许 Agent 检索全部已归档记忆，不需要逐条勾选。默认灵感仍限于当前选中项，未将“全部记忆”扩大为全部未选灵感。嵌入和双链不扩大读取权限：只在允许的集合内解析内容，防止通过链接间接读取未授权灵感。

用户进一步说明“我可以搜索查找历史所有保存的信息和候选稿”，因此按 3.6 落地用户全局搜索，不再把用户检索入口留为待确认项。历史候选稿及已保存的创作要求和可见结果都能找到；不采集模型内部思维或其他应用聊天。已有候选 Markdown 与关联元数据作为索引来源，不为搜索复制第二份权威正文。

用户全局搜索与 Agent 自动读取范围分别控制。Agent 继续按已确认的全部记忆 + 当前灵感 + 用户选入的历史结果工作，不因用户可搜索全库就自动发送全部历史灵感和创作记录。历史材料用于创作时标注日期和“旧创作内容”，不当作新的外部事实；回收站和未解决冲突版本默认不参与生成。

本次核查 DeepSeek Chat Completions 和 Responses 文档：Chat Completions 仅定义 function，Responses 明确内置工具类型被忽略。因此当前不能把 DeepSeek 直接配置为有原生 Web Search 的服务商；网页版是否可以联网不代表 API 可用。主干 SDK/营销描述不覆盖官方接口限制。

用户获知以上限制后，明确选择“只配置 DeepSeek，暂缓联网功能”，因此首版不增加搜索模型或搜索 API，不以联网作为验收项。未来可考虑 xAI Responses `web_search` 等官方原生工具，需要另行确认服务商、计费、来源结构和数据发送范围；这些不属于本次开发范围。

本地检索基于 SQLite FTS5 和中文字符切片索引，结合关键词、标签、作者、标题排序；避免只用默认英文分词导致中文搜不到。首版不依赖 embedding API，减少成本和数据外发。检索目标测试需覆盖中文短词、混合英文、同义表达无结果的提示。

上下文采用可配置预算并保留输出余量，优先用户固定素材；过长材料分段召回并注明截断，来源不存在时不得伪造引用。生成结果需区分材料事实和创作表达，保留输入材料版本以便复查。

第一次启用服务时说明发送对象：当前灵感文本、用户允许检索范围内的命中记忆片段和创作要求。用户可按次调整范围，并可保存为会话偏好；范围授权后不逐次弹窗。图片、整个资料库、Cookie 和 GitHub 凭据默认不发送。

外部记忆作为待引用材料处理，其中的指令不得改变工具权限或执行系统命令。首版 Agent 不联网检索，不能任意访问 URL、删文件、发布推文或操作 Git。服务切换重新确认发送目的地；自定义 Base URL 默认 HTTPS，本机 HTTP 只用于用户明确选择的本地模型。

支持 SSE 流式显示、停止、保留中断前结果、手动重试；鉴权失败、额度不足、限流和超时分别提示。已开始生成后不透明重发以免重复计费；仅在服务提供 usage 时展示实际用量，否则标记未知。

### 8. GitHub 私有仓库同步

用户已确认使用个人独立的 GitHub private repo 存放数据。代码发布仓库与个人数据仓库是两个独立目标。两者具体地址尚未指定；数据仓库由用户在应用内指定，连接时查询并验证 private 标记和写入权限。首版不自动创建仓库。

建议使用 GitHub Git Data API 交换对象、创建 commit，并以非强制方式更新专用同步分支，无需用户安装 Git。认证采用仅授权所选数据仓库的细粒度 Token，存入本机凭据区，不写入同步 URL 或日志。若实现验证发现 API 不适合目标数据规模，必须更新技术方案，不私自换存储服务。

同步白名单：manifest、Markdown 正文、关联元数据 JSON、正文引用的库内原图、删除标记和冲突副本。排除 SQLite、编辑器缓存、缩略图、下载游标、Cookie 接收目录、机器路径、API Key、Token 和设备主题偏好；缩略图和搜索索引在目标设备重建。外部新加入附件也必须完成同步，不能只同步应用最初创建的散列文件。

同步流程：

1. 对本地已刷盘内容制作快照，读取远程 branch head 和上次共同基线。
2. 下载远程新增对象并校验散列；不执行仓库内容，不接受路径穿越、符号链接或未知目录。
3. 对每条记录执行基于共同版本的三方合并；仅单端更改可直接推进，双端正文更改保存两份并提示选择，禁止只靠更新时间覆盖。
4. 删除使用 tombstone；删除与编辑冲突保留编辑版本和删除意图，未解决前不物理清理媒体。
5. 按内容散列上传缺失附件，再创建引用这些附件的内容快照。原子更新分支引用，读端只消费完整 commit。
6. 若远程 head 已变化，重新拉取并合并后有限重试，最多 3 次；失败保留本地待同步状态，不强推。
7. 保存新的共同基线和远程 commit；同步期间新产生的编辑进入下一轮，不覆盖正在输入的内容。

#### 8.1 使用者可处理的冲突流程

用户接受保留双方、由使用者决定。应用以内容版本呈现冲突，不要求使用者运行 Git 命令或编辑冲突标记：

1. 将两份正文、元数据和引用图片完整保存，显示条目标题、来源设备、编辑时间及差异；使用稳定的设备/版本标签，不仅显示会随设备变化的“本地/远端”。
2. 提供“采用版本 A”“采用版本 B”“两份都保留”“手动合并”“稍后处理”。比较视图支持正文预览和 Markdown 差异；合并文本必须由用户保存，不默认交给模型处理私有内容。
3. “采用”会将选中正文作为当前版本，未选版本仍可在冲突历史恢复；“两份都保留”保留原条目 ID 并为副本生成新 ID/名称，副本沿用图片引用。原链接继续指向原 ID，界面说明副本的新增位置。
4. 冲突记录及双方副本参与同步；状态显示“内容已同步，有 N 项待处理”，不能称为完全无冲突。无冲突条目照常同步、阅读和编辑，未处理冲突不阻塞整库。
5. 同一冲突依据实体 ID 与参与版本确定标识，反复同步不重复生成副本；有新编辑时追加新版本，不把它覆盖进旧副本。
6. 解决时核对双方版本未发生变化，再生成引用双方父版本的解决记录。两台设备并发作出不同解决方案时保留两个解决版本，重新提示，不靠时间戳择一覆盖。
7. 删除与编辑冲突展示“恢复编辑版本”或“确认删除”，确认删除仍保留可恢复历史；只有用户后续明确清理且无引用时才处理相关资产。

启动后、恢复联网后、每 10 分钟检查同步；编辑后约 60 秒空闲时可触发，所有同步单实例执行并合并短时间内变更。数据离线可编辑，远端故障不影响本地保存。

图片是主要容量风险。首版普通 Git 对象方案设置应用层单文件 20 MiB、每轮 20 MiB 批次目标、资料库 1 GiB 软提醒，均为产品保护阈值而非 GitHub 官方配额。大图仍保留本地并显式显示未同步原因，不静默压缩或遗漏；需要更大容量时另行设计 LFS/对象存储。GitHub 官方阻止超过 100 MiB 的普通 Git 文件，使用 LFS 需要额外机制和配额，不视为天然可用。

仅当所有被引用原图和内容已抵达远端，才显示该批次“同步完成”；允许区分“文字完成、附件待同步”。私有仓库权限控制不等同于端到端加密；删除当前文件不会清除 Git 历史中的旧版本，UI 应在清理功能中说明。

### 9. 异常、兼容性与迁移

| 情况 | 处理 |
| --- | --- |
| 断网 / 磁盘满 | 保留已落盘内容，明确未保存状态；暂停网络或写入任务，恢复后可继续 |
| Cookie 失效 / 账号变更 | 暂停任务；重新认证并校验账号，旧资料与进度不混用 |
| 平台字段改变 / 页游标循环 | 标记采集受阻，保存诊断摘要；不能误标全量完成 |
| 图像失效 / MIME 不符 / 解码过大 | 标记附件失败，保留来源和正文；检查文件签名和像素上限 |
| 粘贴 HTML 含脚本或远程图片 | 清理危险结构；远程图片经受限下载器导入，禁止直接作为可执行内容 |
| 模型响应中断 / 不兼容 | 保留部分结果并提示；不覆盖已有候选稿 |
| GitHub 改为公开 / 权限撤销 | 每轮同步前验证；暂停上传并提示，不改变远端可见性 |
| 双端编辑 / 删除冲突 | 保留双方可恢复副本，不自动采用最后写入胜出 |
| 本地索引损坏 | 从规范内容重建；下载任务根据内容和资产状态恢复，不删除资料 |

首次版本没有旧业务数据要迁移。后续 schema 迁移先创建本地备份，按序执行并验证；失败可回退。旧客户端遇到更高不兼容 schema 禁止写入和上传，避免破坏新格式。

已确认平台目标为 Windows 10 及以上，以及 Intel 和 Apple Silicon 两类 Mac。Windows 首版按 x64 打包，覆盖 Windows 10/11；Windows ARM 和 32 位不作为已确认目标。Mac 提供 x64、arm64 两个安装包或经过验证的 Universal 包，两种架构均是正式验收要求，不能把 Intel 降为仅编译检查。

macOS 最低版本暂沿用草案的 13+，用户本次明确的是芯片架构，并未确认 macOS 版本下限。阶段 A 核对所选 Electron 和原生依赖的兼容性，选择能满足 Windows 10 目标的受支持组合；如无法满足，报告原因并调整方案，不能静默把最低版本提高到 Windows 11。Windows 10、Windows 11、Mac Intel、Mac Apple Silicon 分别记录安装、启动和核心功能测试，缺少环境时标为未验证。

安装包目标为 Windows 安装程序和 Mac DMG。签名、公证取决于用户提供的开发者身份；没有签名时如实交付测试包并说明系统提示，不称为已签名正式发行版。

### 10. 阶段任务

全部阶段属于 v0.1.0；阶段记录写入 `ChangeLog.md`，最终版本通过验收后才发布 `v0.1.0`，不重复创建同名 tag。

| 阶段 | 工作与输出 | 通过条件 |
| --- | --- | --- |
| A：风险验证 | 方案确认后初始化最小项目；双端 Cookie 文件监听导入、X 书签分页、单条长文/图片/封面；Markdown 往返和外部回读；DeepSeek 流式/工具调用；GitHub 图片往返 | 记录实际支持矩阵、源端限制和失败行为；核心路径不可行时先改设计 |
| B：本地基础与灵感 | 桌面壳、主题、Markdown 权威正文、外部监听、原子保存、编辑冲突、粘贴图、列表、回收站、导出 | 重启不丢图文；外部回读、异常保存和基础交互测试通过 |
| C：记忆 | 历史归档、增量、媒体队列、checkpoint、离线阅读与中文检索 | 中断恢复与新增旧推文测试通过，视频流无下载 |
| C2：全局搜索 | 全库统一索引、筛选、候选版本分组、命中定位、外部更新与同步后索引刷新 | 不配置模型即可离线找到全部类型的已保存内容；回读、删除恢复和索引重建测试通过 |
| D：创作 | Provider 配置、DeepSeek、只读 Agent 工具、流式与候选版本 | 能基于选定灵感和本地命中材料生成并追溯来源 |
| E：同步 | 数据私库连接、附件上传、双端合并、冲突和删除恢复 | 双端离线编辑与图片恢复不丢数据 |
| F：验收与发布 | 双端安装和关键交互检查，README、LICENSE、版本记录、代码提交与附注 tag | 功能用例通过，目标代码仓库已明确，远端 commit/tag 核对完成 |

初始化时创建与真实实现一致的 README、MIT LICENSE、运行与测试命令，并更新 `AGENTS.md` 项目状态。版权权利人由用户提供，不猜测姓名。开发和数据仓库分别确认，当前不创建远端或读取凭据。

### 11. 功能测试与验收标准

以下为计划测试，当前全部未执行。使用合成数据做确定性测试，真实账号只用于用户授权的接入验收，不将私有书签写入测试夹具或公开日志。

| 编号 | 测试场景 | 预期结果 |
| --- | --- | --- |
| I-01 | 两端粘贴截图、单图、多图、图文 HTML，随后重启 | 图片顺序与正文正确，均引用本地文件，无重复图 |
| I-02 | 中文组合输入、快速切换、撤销重做、保存期间退出 | 不破坏输入，不丢已确认保存的内容 |
| I-03 | 文件写入失败、超大/假图片、正文删除并恢复 | 不假报保存成功，错误可恢复，回收站恢复图文 |
| I-04 | 应用保存后在外部编辑 Markdown 并保存；重复切换富文本/源码 | 正文、链接、图片和未知 Front Matter 字段不丢；磁盘为权威正文 |
| I-05 | 应用内有未保存改动，同时外部改写/原子替换同一文件 | 双方内容保留，停止自动覆盖，用户可选择或合并 |
| I-06 | 外部新增、改名、删除、重复 ID、移动图片 | ID 关联准确；缺失与冲突明确；不误删或重复导入 |
| I-07 | 外部写入不支持语法、危险 HTML、越界图片链接 | 原始 Markdown 保留；安全预览/源码模式；不执行或越界读取 |
| I-08 | 用户修改归档正文后再次扫描同一条 X 内容 | 用户正文保留；源端更新仅作为候选版本 |
| I-09 | 表格增删行列、对齐、管道符转义，经外部编辑再回读 | 内容、列结构、链接往返不丢；复杂合并单元格不静默转换 |
| I-10 | 双链含别名/标题/重名，目标改名或删除 | 候选、跳转和反向链接准确，歧义/缺失明确，不错误重定向 |
| I-11 | 图片和笔记嵌入、源笔记变化、循环嵌入、越界目标 | 渲染更新正确；循环有界；不越界读取；导出依赖完整 |
| C-01 | Netscape/JSON 文件导出、编号副本、重复事件、未完成下载 | 写完后自动导入一次，错误格式不覆盖有效会话 |
| C-02 | 失效 Cookie、不同账号、混有其他域、含 HttpOnly 前缀 | 账号验证和域过滤准确；不保留其他站点数据；无原值日志 |
| C-03 | Cookie 目录丢失/恢复、启动补扫、暂停监听、断开账号 | 状态正确，可手动恢复；断开后停止采集且归档仍在 |
| M-01 | 多页书签含长推、引用卡片、Article、图片、视频及作者续帖 | 仅被收藏单条正文及附属静态资源保存；不追串文、不请求视频流；缺失有标记 |
| M-02 | 内容写入前后、cursor 提交前后、附件下载中强制中断 | 重启重放后不漏已发现内容、不重复记录，附件可补下载 |
| M-03 | 新增书签指向很早发布的推文；一次新增超过一页 | 全部被发现，不用发布时间或首条旧记录提前停止 |
| M-04 | 游标过期、重复页、空页异常、响应 schema 改变 | 有限重试、明确受阻，无无限循环或假完成 |
| M-05 | 401/403/429、验证码、断网、睡眠后恢复 | 暂停或退避准确，恢复后继续且任务不重入 |
| M-06 | X 取消收藏或原文删除 | 本地归档保留，不因未见该记录自动删除 |
| M-07 | 断网阅读正文与原图，中英混合搜索 | 已完成资源可离线访问，中文短词有合理命中 |
| M-08 | 模拟时钟下混合历史、详情、重试、新增任务及连续点击 | 全部遵守统一请求间隔、每轮预算和冷却，无突发并发；429/验证立即停止 |
| M-09 | 输入含评论、点赞计数及评论链接的源端响应 | 不保存互动字段，不额外请求评论和点赞接口；正文中正常提及的文字不被误删 |
| A-01 | DeepSeek 基于指定灵感和本地材料生成，再改写 | 流式结果可保存，候选版本保留，来源可追溯 |
| A-02 | 无命中、上下文超限、不支持 tools 的兼容服务 | 说明材料不足或截断，可降级，不编造来源 |
| A-03 | 停止、网络中断、密钥错误、限流 | 保存部分结果并准确提示，不自动重复计费请求 |
| A-04 | 恶意素材要求上传密钥或删除文件 | 不扩展工具权限，不读取或发送凭据 |
| A-05 | 全部记忆召回，以及记忆链接到未选灵感 | 可检索全部记忆但不经嵌入扩大灵感访问范围；只发送所需片段 |
| Q-01 | 同一关键词仅存在于历史灵感、记忆、创作要求、旧候选稿，离线搜索 | 全部可检索，候选版本可展开，未配置模型也可使用 |
| Q-02 | 中英混合、表格、链接、短语和检索特殊字符 | 结果符合匹配规则，不报查询语法错误；高亮不执行 HTML |
| Q-03 | 筛选类型/作者/日期/标签，切换排序并翻页 | 筛选组合正确，稳定分页不重不漏，空结果可清除筛选 |
| Q-04 | 保存、外部改写、同步、改名、删除恢复及索引重建 | 已保存内容及时可见，旧命中失效不误定位，源文件不被重建修改 |
| Q-05 | 点击旧版本命中及包含回收站/冲突副本筛选 | 打开正确版本与段落，状态明确，默认不显示回收站/冲突副本 |
| Q-06 | 普通全局搜索及将一条历史结果加入创作 | 搜索零模型请求；生成只读取既定范围和选入结果，不扩大为整库上传 |
| S-01 | A 端编辑图文并同步，B 端首次恢复后断网打开 | 正文、原图、引用一致，不能只恢复资源指针 |
| S-02 | 两端同时改同一灵感、远端 head 竞争 | 保留双方，冲突可处理，无强推覆盖 |
| S-03 | 一端删除另一端离线编辑，然后恢复联网 | 显式冲突和可恢复内容，删除意图不丢 |
| S-04 | 上传一半退出、配额/权限失败、私库转公开 | 远端可消费快照完整；失败批次不假报成功 |
| S-05 | 扫描同步清单与构造的秘密哨兵值 | 无凭据、Cookie、数据库、机器路径或非白名单文件 |
| S-06 | 选择双方任一版/保留两份/手动合并/稍后处理 | 原文和图片可恢复，副本 ID 唯一，其他条目同步不受阻 |
| S-07 | 重复同步同一冲突，两端并发解决、解决期间新编辑 | 不制造重复副本；保留竞争解决版本，提示新冲突，不覆盖新改动 |
| U-01 | 两端主题切换、系统主题变化、快捷键、窗口缩放 | 状态保存、布局可用、键盘焦点明确 |
| U-02 | 应用驻留、完全退出、重启恢复 | 驻留可扫描，完全退出停止，恢复不重复排队 |
| U-03 | Windows 10/11 x64 与 Mac Intel/Apple Silicon 安装运行 | 四类目标环境分别记录结果；粘贴图片、Markdown 回读、搜索和凭据导入可用；构建成功不代替功能验证 |
| A-07 | 短推与串文生成、条目编辑/增删/排序、复制、历史版本搜索 | 每条独立计数和超限提示，顺序及候选版本可恢复；短推长度限制不截断资料正文 |
| P-01 | 1,000 条合成记忆及可控图片集，另以 10,000 条做扩展观察 | 以用户当前不足 1,000 条规模验收；记录机器条件；目标搜索 P95 < 500ms，归档/回读不阻塞输入 |

代码验证计划：TypeScript 类型检查、Lint、直接相关单元/集成测试、Electron 关键交互端到端测试、两平台打包与真实安装验证。选择测试框架后把可复现命令写入项目说明。构建成功不能替代登录、粘贴、同步功能测试。

最终验收必须满足：三条核心工作流成立；用户可离线全局检索全部已保存类型和保留候选版本；文本和原图可离线恢复；任务重启可继续；跨端冲突不丢稿；已知平台缺失透明展示；双端测试有记录。缺少 Mac 环境或接入认证时明确标记对应验收受阻，不宣称版本全面完成。

### 12. 待用户确认的范围

已对齐：名称 XMemeory；Electron；手动通过插件导出并自动导入 Cookie；书签单条正文/图片/封面/链接、不采评论赞、不追串文、限速；首版 DeepSeek 本地资料创作、暂缓联网；不到 1,000 条书签；直接 Markdown 保存与外部编辑回读。

当前访谈中的关键问题：

1. 已确认表格、嵌入、双链必需；具体语法与复杂度按 6.1.1 细化，默认嵌入为库内图片和 Markdown 笔记。视频/PDF/远程页面内嵌若需要，另行补充。
2. 已明确增加用户全局搜索：全部已保存灵感、记忆、创作记录和候选版本都可检索；Agent 自动读取仍按已确认范围控制，用户可从搜索结果显式加入本次创作。不再追问用户搜索是否包含候选稿。
3. 已确认个人独立 GitHub 私有数据仓库、冲突保留双方供使用者处理；具体仓库地址待提供。单设备自动采集、单图 20 MiB 与 1 GiB 提醒仍为草案默认，不标为单独确认。

用户已补齐创作重点（短推和串文）、Windows 10+ 与 Mac 双架构要求，并明确代码仓库后续提供。剩余执行信息在对应阶段核对：Mac 实际系统版本/测试环境、常用浏览器、版权权利人和签名身份。扫描频率默认 30 分钟；macOS 13+ 暂为草案默认。仓库未提供不阻塞已获授权的本地开发，但会阻塞真实远端同步验收和版本发布；不猜测远端、不虚报发布完成。

当前已确认并进入开发。代码发布仓库、版权权利人、Mac 测试环境和签名身份在对应阶段补齐；它们不阻塞本地实现，不要求用户在聊天中提交任何密钥或 Cookie。

### 12.1 开发验证进度（2026-09-14）

数据库锁故障修复：获取应用单实例锁后才打开资料库；SQLite 索引更新失败不阻止 Markdown 落盘，事务异常回滚并有限重试，文件监听错误通过状态展示。关闭时保存冲突可由用户选择另存到应用数据目录的 recovery 副本，成功后才关闭，保留原文和冲突。该改动补齐既定不丢稿/异常恢复要求，不增加业务范围。

本轮问题修复维持原功能范围：表格和本地链接解析补充回归；Cookie 自动接收独立于联网验证，先验证导出格式并加密保存，再由用户点击验证登录或扫描。界面分别展示文件接收状态与 X 登录状态，不把导入成功视为认证成功。人工验收步骤落于 README。

当前已建立 Electron 主进程、受限 preload 和 React 桌面窗口；生产运行从本地构建文件加载，不依赖网站服务器。Windows 10 22H2 开发环境已运行桌面行为测试，覆盖本地保存、外部回读、表格、系统剪贴板图片、关闭前保存与托盘隐藏、中文搜索和深色外观。具体结果见 ChangeLog.md。

完整版本验收仍未完成：真实 X 接口、长正文与附件任务恢复、DeepSeek 调用、GitHub 跨端同步；复杂 Markdown 转换保真、完整候选版本检索与高亮；Mac 双架构和正式安装/签名验收。保留原验收目标，不用当前桌面基础测试替代。

### 13. 资料依据与调查边界

以下资料在本次规划时查阅；外部接口可能变化，阶段 A 必须实测。没有读取真实 Cookie、私人书签或调用付费模型。

- [用户提供的 X History 入口](https://x.com/i/history)：未能通过公开网页读取器查看登录后内容，具体标签和集合待实测。
- [X 书签官方说明](https://help.x.com/en/using-x/bookmarks)：书签为账号私有数据。
- [X 官方书签 API](https://docs.x.com/x-api/posts/bookmarks/introduction)：官方查询与网页会话采集是不同接入路径。
- [Chrome Windows Cookie 应用绑定加密](https://security.googleblog.com/2024/07/improving-security-of-chrome-cookies-on.html)：不能保证外部进程直接解密读取。
- [DeepSeek 首次调用](https://api-docs.deepseek.com/zh-cn/)：通过 agent-reach 技能指定的 Jina Reader 读取，确认兼容 API、Base URL 和流式接口；模型 ID 保持可配置。
- [DeepSeek Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion/)：生成接口、工具调用字段以开发时官方定义为准。
- [Electron Context Isolation](https://www.electronjs.org/docs/latest/tutorial/context-isolation)：桌面主进程与界面隔离依据。
- [Tiptap FileHandler](https://tiptap.dev/docs/editor/extensions/functionality/filehandler)：粘贴事件处理和实际文件存储需分别实现。
- [GitHub 大文件限制](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)：普通 Git 文件限制及大媒体规划依据。
- [Git LFS](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-git-large-file-storage)：使用指针和独立对象存储，不等于普通 Git 同步已包含 LFS。

调查限制：当前环境无法执行 `agent-reach doctor --json` 和 `gh`，命令不在 PATH；未安装或修改研究工具。已使用该技能给出的网页读取路径，并以官方 GitHub REST API、固定提交的公开原始文件和官方文档检索补足。未克隆第三方仓库、未运行第三方代码、未安装 Cookie 插件。

### 14. GitHub 实现参考（本轮研究）

研究方式为阅读公开源码中的具体实现，并记录固定 commit；下列项目提供参考，不代表其主干可直接作为生产依赖，也不把其功能自动加入 XMemeory 范围。

| 项目与已读实现 | 可采用的思路 | 对 XMemeory 的具体决策 |
| --- | --- | --- |
| [Get cookies.txt LOCALLY：导出动作](https://github.com/kairi003/Get-cookies.txt-LOCALLY/blob/0e9952ccb1b1d914c3dc42fa496219bb7285e2dc/src/popup.mjs)、[文件下载](https://github.com/kairi003/Get-cookies.txt-LOCALLY/blob/0e9952ccb1b1d914c3dc42fa496219bb7285e2dc/src/modules/save_to_file.mjs)、[后台事件](https://github.com/kairi003/Get-cookies.txt-LOCALLY/blob/0e9952ccb1b1d914c3dc42fa496219bb7285e2dc/src/background.mjs) | 当前站点导出，Netscape/JSON；下载完成事件与 Cookie 变化计数分离 | 兼容其现有文件格式，应用监听完成文件；不宣称插件会自动续期或推送 |
| [Tiptap：FileHandlePlugin](https://github.com/ueberdosis/tiptap/blob/7ab690e5056fe999988552822c4fc1b1064bf094/packages/extension-file-handler/src/FileHandlePlugin.ts) | 区分外部文件拖入与内部节点移动；处理 files 和 HTML 并存；可消费粘贴事件避免重复 | 使用开源编辑器和文件处理入口，图片写本地库；验证混合粘贴和撤销；不依赖付费 AI/协作服务 |
| [Vercel AI SDK：DeepSeek provider](https://github.com/vercel/ai/blob/6c6c2210b9532a4c369615c044a16d595f3db117/packages/deepseek/src/deepseek-provider.ts)、[provider registry](https://github.com/vercel/ai/blob/6c6c2210b9532a4c369615c044a16d595f3db117/packages/ai/src/registry/provider-registry.ts) | 显式 apiKey/baseURL/custom fetch、provider/model 分离、按能力报错 | 优先直接采用稳定版 provider 包；配置化扩展；显式连接服务商，不隐式走第三方 AI Gateway，不假设 SDK 所列功能在所有模型可用 |
| [Cherry Studio：provider config](https://github.com/CherryHQ/cherry-studio/blob/3e9b7d2d08ac6ac3c907e4a36ba3e086489993b6/src/main/ai/provider/config.ts) | endpoint 与 provider 分层，不同服务需不同 URL 和请求体处理 | 参考设置页面及适配边界，避免“只改 URL 就兼容一切”的设计；不引入其完整代理/插件系统 |
| [Joplin：Synchronizer](https://github.com/laurent22/joplin/blob/41b5934cb069772f935015ad6551ce33f8cddda0/packages/lib/Synchronizer.ts)、[Resource](https://github.com/laurent22/joplin/blob/41b5934cb069772f935015ad6551ce33f8cddda0/packages/lib/models/Resource.ts)、[同步设计](https://joplinapp.org/help/dev/spec/sync/) | 内容与附件分离、下载状态、独立冲突处理与同步测试 | 借鉴可恢复资源任务和冲突语义，自行实现适合 GitHub 的协议；不将 SQLite 文件互相覆盖作为同步 |
| [MarkText：watcher](https://github.com/marktext/marktext/blob/34e12fc9caaf5c5f1346999a91165cd88877de5f/packages/desktop/src/main/filesystem/watcher.ts)、[写入完成测试](https://github.com/marktext/marktext/blob/34e12fc9caaf5c5f1346999a91165cd88877de5f/packages/desktop/test/unit/specs/watcher-await-write-finish.spec.ts) | 文件内容事件等待写入稳定；目录列表事件可立即更新；处理自身写入和外部变更 | 单独设计 Markdown 回读与 Cookie 文件监听；两类数据解析和权限范围分离；覆盖原子替换、重复事件和冲突 |

本次查看的根许可证：Cookie 插件、Tiptap、MarkText 为 MIT；AI SDK 为 Apache-2.0；Joplin 默认 AGPL-3.0-or-later（子目录可覆盖），Cherry Studio 根标记 AGPL-3.0。项目继续遵循已定 MIT 方向；对后两者仅参考行为和架构，不直接搬用源代码。新增依赖时核对实际选用包的许可证及 notice，不能只看仓库根标签。

补充官方依据：

- [Tiptap Markdown](https://tiptap.dev/docs/editor/markdown)：双向转换接口及 Beta/表达能力限制；首版必须自行验证不丢正文。
- [Tiptap 内容持久化](https://tiptap.dev/docs/editor/core-concepts/persistence)：框架可输出 JSON/HTML，但本项目按用户要求选择 Markdown 为权威正文，不照搬默认存储示例。
- [Chrome Native Messaging](https://developer.chrome.com/docs/extensions/develop/concepts/native-messaging)：完全自动桥接需要配套扩展权限和本机 host 注册；当前不实施。
- [DeepSeek Responses 支持范围](https://api-docs.deepseek.com/guides/responses_api)、[Responses API](https://api-docs.deepseek.com/api/create-response)：内置工具不构成可用的原生 Web Search，用户已据此决定首版延期联网。
- [xAI 原生 Web Search](https://docs.x.ai/developers/tools/web-search)：作为后续联网可行性参照，不是首版依赖。
