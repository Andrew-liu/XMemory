# XMemeory 开发指南

## 作用域与项目状态

- 本文件适用于 XMemeory 项目根目录及全部子目录；同时遵循上级工作区的 `AGENTS.md`。子目录存在更具体的规则时，操作前先读取。
- 沟通、开发说明和交付总结默认使用简体中文；代码标识符遵循所选语言及项目惯例。
- 当前为 v0.4.0 本地测试版本：Electron + React + TypeScript，Markdown 为权威正文，SQLite 为可重建本地索引；已初始化本地 Git 并连接用户指定的代码仓库 Andrew-liu/XMemory；首次提交与标签推送待完成。
- 项目名称不等于产品需求。未确认前，不预设产品功能、技术栈、数据库、外部服务或部署方式。

## 开始开发前

1. 明确本次任务的目标、边界和验收标准；先从已有文件寻找答案，仅在关键决策确实阻塞时向用户提问。
2. 检查目标目录及其上级的规则文件，阅读相关项目说明、依赖清单和配置。
3. 确认是否属于 Git 仓库；若是，检查仓库根目录和工作区状态，保留用户已有修改。若不是，如实说明，不自动初始化或连接远程仓库。
4. 先搜索相关定义、调用方、数据结构和测试，再确定最小修改范围。
5. 信息不足但可继续时，简要说明假设；不得把假设写成既定需求或已实现功能。

## 规划与开发确认

- 每个版本开发前先阅读已有规划方案，结合当前项目状态整理详细设计案，写入项目根目录的 `Design.md`，再向用户展示并讨论；不得只在对话中描述计划而不落地文件。
- `Design.md` 按版本分节，包含版本号、背景与目标、功能范围与非目标、主要使用流程、技术方案与模块职责、涉及的接口和数据结构、异常与边界处理、兼容性及数据迁移影响、阶段任务、功能测试用例和验收标准。确实不适用的部分说明原因。
- 每个版本设计标明草案或已确认状态，保留历史版本设计；确认后记录确认范围，后续变更同步更新，不将未获确认的设计标为已确认。
- 必须与用户就方案达成明确一致后，才能开始编码、修改功能或执行项目初始化；未确认时可进行只读调查及方案整理，不以默认假设或用户未回复代替确认。
- 已确认方案范围内的工作可持续执行，无需逐步重复确认；若功能范围、技术路线或验收标准发生实质变化，先更新 `Design.md` 并再次达成一致后再实施相关变更。
- 每个版本依次执行：详细设计落地 `Design.md` → 用户确认 → 开发实现 → 完成功能测试 → 落地 `ChangeLog.md` → 提交 GitHub 并打 tag。

## 阶段版本记录

- 每个开发阶段都必须在项目根目录的 `ChangeLog.md` 中有对应记录，准确描述该阶段当前版本已经具备的功能。
- 按版本或阶段分节，记录版本号或阶段名称、日期、当前版本功能、相对上一阶段的新增或变更、验证结果，以及已知限制或未完成事项。
- 当前版本功能应说明用户实际可以完成什么，不能只列修改的文件；计划中但尚未实现的功能不得写成已完成功能。
- 每个开发阶段完成实现并执行对应功能测试后，才在 `ChangeLog.md` 中落地该阶段的版本记录；按 `Design.md` 的测试用例和验收标准记录实际结果，不能以编译或 Lint 通过代替功能测试。
- 功能测试失败或受阻时，如实说明失败项、未验证项和原因，不宣称阶段完成或发布版本；阶段交付前核对记录与实现一致，缺少对应记录时不得视为交付完成。
- 使用统一文件名 `ChangeLog.md`，保留各阶段历史记录，新阶段记录放在前面，不覆盖已有版本说明。

## 项目初始化

- 首次功能开发时，根据实际需求选择最小可用的技术栈，并简要说明选择依据；已有技术栈确定后优先沿用。
- 只创建当前功能需要的目录、模块与配置，不提前搭建无实际用途的分层、插件系统或服务。
- 确定依赖管理工具后保持一致，维护对应锁文件；新增依赖前先检查标准库和现有依赖是否足够。
- 配置文件使用相对路径或环境变量，不写入机器专属路径。需要环境配置示例时，只提供占位值。
- 技术栈落地后更新本文件，补充实际目录职责、运行入口、依赖安装、开发启动、测试、Lint、类型检查及构建命令；未配置的能力明确标为未配置，不编造命令。

## README 与开源协议

- 项目必须包含根目录 `README.md`，在项目初始化时创建，并随功能和构建流程变化同步更新。
- README 的项目标题必须居中，使用 `<h1 align="center">XMemeory</h1>`；标题下方使用居中的 Badge 区域展示项目状态。
- Badge 至少包含 MIT 协议标识；版本、构建状态等 Badge 根据实际仓库及工作流配置添加，不展示虚假的通过状态或无效链接。
- README 必须描述项目用途、主要功能、环境与依赖要求，以及可复现的安装、配置、构建和运行方法。命令须与实际项目一致；尚未实现的功能或尚未配置的构建流程应明确说明。
- 默认采用 MIT 开源协议，项目根目录提供标准 MIT 文本的 `LICENSE`，README 中说明并链接该文件；版权年份和权利人根据实际信息填写，不臆造身份。

## 版本与 GitHub 发布

- Git 提交身份：作者名称 `Andrew-liu`，邮箱 `1095511864@qq.com`（用户指定后续提交沿用）。使用命令级身份参数，不修改全局 Git 配置。

- 每个交付版本都必须提交到 GitHub，并创建和推送对应 Git tag；这是本项目的既定发布要求，在已确认的开发方案及已明确的目标仓库范围内执行，无需对每次常规提交、推送和打 tag 重复请求许可。
- 版本号默认遵循语义化版本，tag 使用 `vX.Y.Z` 格式；项目版本信息、`Design.md`、`ChangeLog.md` 和 tag 必须一致。
- 发布前完成适用验证，核对 README、当前版本功能说明及协议文件，检查 Git 状态、提交范围、敏感信息与大文件；仅提交本次版本相关内容，不夹带用户无关修改。
- 将版本内容提交后，在对应提交上创建附注 tag，推送目标分支及该 tag 到已确认的 GitHub 仓库；不使用批量推送全部 tag 的方式发布。
- 发布后核对远程分支和 tag 指向预期提交；提交、tag 和远程核对全部完成后才可声明版本已发布。
- 若仓库尚未初始化、远程目标未明确或认证不可用，先完成本地可验证的准备并说明缺失信息；不得猜测 GitHub 账号、仓库或公开可见性。仓库初始化及远程创建或连接应纳入已确认方案。
- 不覆盖或移动已发布 tag，不强制推送，不修改 Git 配置；发布失败时如实报告本地和远程状态，避免重复创建版本。

## 日常开发约定

### 当前运行与验证

- `src/main/`：桌面主进程、本地文件、凭据、采集、模型和同步；`src/preload/`：受限 IPC 桥接；`src/renderer/`：桌面窗口内容；`tests/`：核心行为与真实 Electron 窗口测试。
- 安装：`pnpm install`，随后 `pnpm exec install-electron`；开发：`pnpm dev`；构建并运行：`pnpm build`、`pnpm start`。
- 类型检查：`pnpm typecheck`；核心测试：`pnpm test`；桌面测试：先 `pnpm build` 再 `pnpm test:desktop`。
- 长文选题专项：`pnpm test:topics`（合成资料库，含图片剪贴板验证）；需先构建。
- 灵感日历专项：`pnpm test:calendar`；单实例与退出恢复：`pnpm test:recovery`；无编辑退出：`pnpm test:clean-exit`；记忆阅读：`pnpm test:memory`。均启动真实 Electron 窗口，先执行构建；可用 `XMEMEORY_TEST_EXE` 指定打包后的程序。
- 打包：Windows 使用 `pnpm pack:win`；Mac 环境使用 `pnpm pack:mac`。打包命令存在不等于跨平台验收或签名已完成。Lint 尚未配置。
- 产品是安装到本机的桌面 App。开发服务器仅用于 Electron 开发热更新；生产窗口加载打包内的本地文件，不部署网站。

- 优先小范围、可审查的修改；修改前读取现有内容，保持编码、换行、缩进和命名风格。
- 不做无关重构，不覆盖或回滚用户修改，不为当前任务额外生成无必要的文档。
- 按职责组织模块；仅在存在明确复用需求时抽象，避免为了预想的未来需求增加复杂度。
- 新增行为应包含必要的输入校验、错误处理及与规模相匹配的测试。错误信息应有助于定位问题，但不得暴露敏感数据。
- 变更接口、配置或持久化格式前检查所有使用方；不兼容变更应说明影响，重要数据变更应提供可恢复方案。
- 临时脚本、日志和下载缓存放入上级工作区的 `trash/` 任务子目录，动态解析实际路径；结束后仅清理本次生成且确认无用的中间文件。

## 数据与安全

- 不读取或输出任务无关的凭据、私有内容及 `.env`；不得把密钥、令牌或真实隐私数据写入代码、测试、日志或版本控制。
- 对外部输入、文件路径、URL 和第三方响应进行校验；网络请求设置超时，重试应有上限且仅用于适合重试的操作。
- 若功能涉及记忆、笔记、对话或其他用户数据，先明确保存范围、读取权限和删除行为；测试使用合成或脱敏数据，向外部服务发送真实数据须有用户授权。
- 持久化操作尽量幂等；重要状态文件采用原子替换，数据库写入按需使用事务。不得静默覆盖或删除用户数据。
- 引入依赖或外部服务时，说明其用途及必要配置，不默认启用遥测或上传项目内容。

## 验证与交付

- 根据实际技术栈执行最小充分验证：语法或编译检查、直接相关测试，以及已配置且适用的 Lint、类型检查或构建；完整测试成本合理时执行。
- 修复缺陷时优先增加能复现问题的回归测试；测试应验证可观察行为和关键边界，不仅重复实现细节。
- 若涉及 Web 界面，检查关键交互与控制台错误；若涉及持久化，验证读写、失败处理及数据兼容性。
- 纯文档修改检查内容准确性、Markdown 结构、引用路径及是否包含机器专属信息，无须为了文档新增测试框架。
- 不声称通过未运行的检查，不删除有效断言或跳过测试来掩盖失败；环境阻塞时说明原因及未验证范围。
- 同一问题最多进行三轮有依据的修复尝试，仍未解决则报告根因和剩余问题。
- 版本提交、推送和标签按“版本与 GitHub 发布”执行；非版本交付的临时修改不自动发布。
- 完成后简要说明修改内容、关键文件、实际验证结果和未完成事项。
