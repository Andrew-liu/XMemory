# ChangeLog

## v0.4.0 首次源码提交准备（2026-09-22）

- 用户指定代码仓库 `Andrew-liu/XMemory`；准备提交现有桌面 App 功能及 `v0.4.0` 附注标签，不包含个人资料、依赖及构建产物。
- 本轮类型检查、107/107 单元测试和生产构建通过，暂存内容检查通过。各功能及桌面专项验证详见下方记录。
- 本条记录提交准备；远端发布结果以分支和标签核对为准。

## v0.4.0 跳过未变化文件下载（2026-09-21）

- 同步读取远端目录树后，按 Git blob SHA 比较本地字节；内容相同直接复用，不再逐个下载。仍检查分支和目录，不因远端提交未变化而忽略本地编辑。
- 本地修改或文件缺失时，优先复用已校验的远端内容缓存；缓存缺失或损坏回退下载。有效 Git SHA 对应的下载内容会进行完整性校验，失败不写入正文。
- 缓存位于资料库 `local/sync-cache/`，按仓库与分支隔离，不参与同步。成功同步后回收旧缓存；失败不推进同步基线，可复用此前成功取得的内容继续重试。
- 同步结果显示下载、复用本地及缓存命中的文件数量。原有未保存编辑保护、冲突、删除恢复及提交非强制更新策略保持。
- 验证：类型检查、107/107 单元测试及生产构建通过；新增测试覆盖相同文件零下载、本地修改上传、远端单文件变化、缓存缺失/损坏、双端冲突、图片、提交失败后重试、哈希错误及缓存隔离。首次全套运行曾遇到独立采集测试的临时文件 EPERM，未修改该逻辑，重跑全部通过。
- 真实 Electron 和打包 EXE 连续同步专项通过：第一次仅下载新增内容，第二次零下载，远端修改后只下载该文件，界面统计与请求吻合。测试使用合成 API 和独立资料库，未读写真实私库。
- Windows 测试包：`release/sync-cache-0.4.0-20260921/win-unpacked/XMemeory.exe`，未签名，保留整个目录；Mac、真实双设备及大资料库性能待验收。项目未连接代码仓库，未提交、推送或打 tag。

## v0.4.0 完整多模型切换（2026-09-21）

- 灵感创作实际使用当前选中的服务，不再固定 DeepSeek。支持 DeepSeek 与 OpenAI Chat Completions 兼容服务；每份配置独立保存名称、HTTPS Base URL、模型名称及加密 Key。
- 设置页支持新增、编辑、切换和删除服务，至少保留一份配置；切换、添加或离开设置前保存修改，失败则保留当前编辑。Key 留空保持原值，删除配置清理对应本机 Key。
- 创作面板可直接选择服务和模型，生成时禁用切换；主进程固定本次配置快照并拒绝生成期间修改配置。缺 Key 或无效当前服务时明确报错，不回退到其他模型。
- 候选稿保存生成时的服务 ID、名称和模型 ID，历史列表与候选稿正文页显示来源；旧稿显示未记录。历史内容不随服务删除或改名变化。
- 校验服务 ID 唯一、当前服务有效、Key 归属正确，禁止使用 GitHub、Cookie 等保留凭据名称。模型配置与 Key 不进入资料库同步。
- 验证：类型检查、98/98 单元测试及生产构建通过；真实 Electron 多模型专项验证三份配置的实际 URL、model 和 Authorization，覆盖切换前保存、缺 Key 不回退、流式输出、停止、生成锁定、来源记录、重启、删除 Key、非法地址阻止切换及保留 ID 拒绝。既有创作专项回归通过。所有模型请求为合成接口，未使用真实 Key 或消耗额度。
- Windows 测试包：`release/multi-model-0.4.0-20260921-r2/win-unpacked/XMemeory.exe`。未签名；保留整个目录。原生 Anthropic/Gemini/Responses 协议、模型目录自动发现不在本轮范围；所选模型需支持流式输出和工具调用。真实服务和 Mac 待用户验收。
- 最终打包 EXE 的多模型专项及无编辑退出专项均通过。
- 未连接代码 Git 仓库，未提交、推送或打 tag；不将个人数据仓库作为代码发布目标。

## v0.3.0 同文件冲突链收敛修复（2026-09-21）

- 修复同一个文件反复出现“原文又有变化，已创建新的比较版本”且冲突数量持续增长的问题。冲突现在按 `noteId` 收敛，同一个文件同时只保留一条活动比较。
- 资料库启动或刷新时自动整理既有重复冲突：保留最新待合并版本，将版本 A 重建为当前磁盘内容；被取代的记录完整保存到 `local/`，并在 `conflicts/` 写入可同步的 `superseded` 标记，不删除任何历史版本。
- 新同步冲突会替换同文件旧活动记录；处理期间文件再次变化时，只刷新这一张比较卡并要求重新确认，不再追加新卡。重新确认后可采用 A、采用 B、两份保留或手动合并。
- 验证：TypeScript 类型检查、91/91 单元测试和 Electron 生产构建通过；真实 Electron 冲突专项通过，在同一个文件注入 7 条历史冲突后界面只显示 1 张最新比较卡，采用 B 成功且无页面错误。测试使用隔离合成资料库，未直接修改用户正式笔记或 GitHub 私库。
- `electron-builder` 连续三次在解压 Electron 后将 `win-unpacked.tmp` 重命名为 `win-unpacked` 时被系统以 `EPERM` 拒绝；未把不完整临时目录作为交付版本。最终使用项目依赖中的官方 `@electron/asar`，将本次生产构建封装进上一版已验证的同版本 Electron 运行时，并通过打包 EXE 的真实冲突专项。Windows x64 解压版：`release/conflict-chain-fix-20260921-final/win-unpacked/`。

## v0.3.0 同步编辑竞态与冲突假死修复（2026-09-21）

- GitHub 同步完成远端读取后、开始本地合并前检查编辑器未保存状态；若用户在同步下载期间开始编辑，本轮同步会安全延后，不改写本地文件、不上传远端版本。
- 编辑器保存检测到外部修改时仍完整保留双方版本；冲突落盘后自动回载磁盘当前版本并解除待保存状态，不再循环执行同一次失败保存，侧栏、冲突页和关闭流程均可继续使用。
- 普通磁盘、权限或格式错误仍保持未保存状态；只有确认双方已写入冲突记录时才解除编辑锁。冲突中的当前版本为外部/同步后文件，待合并版本为编辑器内容。
- 验证：TypeScript 类型检查、87/87 单元测试、Electron 生产构建通过；真实 Electron 冲突解锁、关闭恢复和无编辑退出专项通过。测试确认本地文件、编辑器版本、冲突记录及导航行为，均使用隔离合成资料库，未操作用户真实数据或 GitHub 私库。
- Windows x64 未压缩测试目录：`release/conflict-unlock-fix-20260921-ready/win-unpacked/`；打包 EXE 的冲突解锁和正常退出专项均通过。未生成 ZIP 或安装器，运行时须保留整个目录。

## v0.3.0 完成时间差异自动合并（2026-09-21）

- 正文、完成状态及其他全部附加字段一致，仅 `completedAt` 不同时不再生成待处理冲突，自动采用较早的完成时间作为共同版本。
- 已经存在的同类冲突会在资料库刷新时自动归档；归档前保留完整恢复记录。若较早时间来自待合并版本，会安全写回当前 Markdown。
- 正文、标题、完成状态、日期、自定义字段或其他任意字段不同仍保留冲突；缺失或无效时间不自动合并。
- 验证：TypeScript 类型检查、86/86 单元测试和 Electron 生产构建通过。测试覆盖较早时间来自任一端、历史冲突自动归档及真实字段差异不被吞掉。未直接操作用户资料库或真实 GitHub 私库。

## v0.3.0 启动时自动同步（2026-09-21）

- App 每次完成资料库和窗口初始化后检查 GitHub 私库配置；仓库地址与本机加密 Token 均已配置时，在后台主动执行一次完整同步。
- 启动同步不阻塞窗口打开；网络或权限失败沿用设置页同步状态提示，本地编辑仍可使用。缺少仓库或 Token 时不发送请求。
- 启动同步、手动同步与既有每 10 分钟定时同步共用单实例保护；启动同步结束后重新计算定时间隔，避免短时间重复同步。
- 验证：TypeScript 类型检查、84/84 单元测试和 Electron 生产构建通过。新增测试覆盖完整配置仅触发一次、缺少任一配置不触发、同步失败不阻塞初始化。未使用真实 Token 或私库进行外部读写。

## v0.3.0 相同正文冲突与差异展示修复（2026-09-20）

- 原因：同步比较整份 Markdown 文件哈希，而界面只突出正文差异；Front Matter 差异或 CRLF/LF、BOM、YAML 表示差异都可能导致两列正文相同。
- 双方正文及完整附加字段等价时，不再因为换行或 YAML 表示差异新增同步冲突。正文空格、标题、日期、状态、自定义字段等实际差异仍保留。
- 新同步冲突保存完整 Markdown，界面展示附加字段差异；纯空白正文差异显示可见符号。“采用 B”和“两份都保留”包含完整 Front Matter，不再只替换正文。
- 可证等价且当前文件仍匹配的历史冲突，先保存 local 恢复副本，再写 resolved 标记。信息不足的旧记录明确提示，继续保留，不因正文相同自动删除。
- 验证：类型检查、78/78 单元测试、生产构建通过；真实 Electron 及打包 EXE 冲突专项通过，验证等价归档、字段差异、旧记录提示、空白差异及采用 B 保存自定义字段。使用合成资料库，未直接清理用户真实冲突。
- Windows 测试包：`release/conflict-diff-fix-20260920/win-unpacked/XMemeory.exe`；包含此前选题和同步网络修复，未签名。真实私库和 Mac 未验证；代码仓库未配置，未推送或打 tag。

## v0.3.0 GitHub 连接中断修复（2026-09-20）

- 私库检查和同步改用专用 Electron 网络会话，沿用系统网络配置，省略 Cookie 且禁止重定向；只允许 GitHub API HTTPS 地址，Token 保存方式不变。
- 请求及响应正文读取均处理 terminated、超时和网络断线。读取失败最多重试两次，等待 1/2 秒，每次上限 30 秒；写入上限 60 秒，不自动重试，并提示远端结果可能未确认。401/403/404 保留原有权限提示。
- 显示身份、仓库、分支、目录、文件及写入步骤，错误不输出凭据或响应正文。当前仍逐个下载远端文件，没有新增文件缓存或增量下载。
- 验证：类型检查、73/73 单元测试、生产构建通过；真实 Electron 及打包 EXE 的 GitHub 专项通过，覆盖合成凭据保存/重启、401/404、响应正文中断后恢复、持续中断有限重试、Cookie/重定向隔离。未使用真实凭据读写私库。
- 本机公开 API 探测：系统代理已配置，Node 和 Electron 两通道均返回 200，单次耗时分别为 1717 ms / 327 ms；不能据此认定此前中断的确切原因，亦不代表私库认证成功。
- 测试包：`release/github-network-fix-20260920/win-unpacked/XMemeory.exe`，保留整个目录，未签名；包含选题功能。真实私库与 Mac 待验收；未配置代码仓库，未推送或打 tag。

## v0.3.0 长文选题清单（2026-09-20）

- 侧栏新增“选题”，与灵感、记忆并列。输入标题按 Enter 或点击加号创建；顶部“新选题”可直接建立空白选题。每个选题是一条待办，打开后在同一篇 Markdown 中编写大纲与长文。
- 勾选完成后标题划线，移到未完成选题下方；可撤销，组内按创建时间倒序，编辑不会打乱顺序。勾选前保存正在编辑的正文，失败时保留原状态。
- 选题保存到 `topics/`，加入全局搜索及类型筛选、外部编辑回读、回收站、导出、双链和图片预览复制；私库同步包含选题正文和附件。同步冲突保留双方正文及完成状态；采用远端选题时同时采用其标题和完成状态。
- 验证：`pnpm typecheck`、68/68 单元测试、`pnpm build` 通过。`pnpm test:topics` 在真实 Electron 及打包 EXE 均通过：快速创建、空标题、分组排序、划线/撤销、编辑后立即勾选、表格、双链跳转、重启保留、外部回读、搜索、删除恢复、粘贴图片、大图复制、深色主题，无页面错误。同步使用合成远端 API 验证上传范围及冲突采用/保留双方，未访问真实私库。
- 回归：灵感日历、无编辑退出、图片复制专项通过；打包 EXE 的无编辑退出专项通过。测试均使用独立合成资料库，不修改用户现有内容；图片专项会覆盖系统剪贴板。
- Windows x64 本地测试包：`release/topics-0.3.0-20260920/win-unpacked/XMemeory.exe`，须保留整个目录；未签名、未生成安装器。未验证 Mac 和真实私库双设备同步；不包含长文 AI 生成或独立子任务。双端使用选题需升级到支持 `topics/` 的版本。
- 本地实现及验收完成，尚未发布：当前目录不是 Git 仓库，代码发布远端尚未确认，因此未执行提交、推送或打 tag；用户私有数据仓库不作为代码发布仓库。

## v0.2.0 同步冲突、灵感排序与高亮修复（2026-09-20）

- GitHub 同步遇到元数据并发差异时不再中断整轮同步：双方内容继续保存在本地恢复记录，本地版本作为共享快照上传；同步成功后为完整最终快照保存共同基线，避免未修改内容在后续同步中反复产生正文冲突。
- 同一天的灵感按“未完成在前、已完成在后”展示，两组内部继续沿用已有 `sortOrder`、创建时间和 ID 的稳定顺序；跨天拖动和同日排序行为保持不变。
- 全局搜索在标题和摘要中高亮全部空格分隔的命中词；冲突页高亮双方正文从首个差异到最后差异的变化区段。高亮使用 React 文本节点，不解析或注入 HTML。
- 实际验证：TypeScript 类型检查通过；完整单元测试 63/63 通过；Electron 生产构建通过。
- 限制：未连接真实 GitHub 私库执行双端同步，远端 API 与已有待处理冲突需要用户在新版中完成一次真实同步和人工处理；本次不自动删除或修改现有冲突记录。

## v0.2.0 GitHub Token 401 诊断与保存验证（2026-09-19）

- 保存前校验个人访问 Token，清理首尾空白；拒绝名称、Bearer 前缀、引号、掩码和隐藏字符，不在错误里回显密钥。兼容细粒度、经典及旧式 40 位十六进制 Token。
- 设置显示保存的 Token 类型和最近保存时间，明确本机保存不等于 GitHub 认证成功；加密写入与解密回读验证成功后才更新内存，失败或非法输入不静默覆盖内存旧值。
- 只读“检查连接”先调用 `/user`，认证成功后再检查私库及分支。401 身份检查失败时不继续请求仓库；404 私库不可访问时可显示已认证账号和细粒度授权指引，不输出 Token。
- 实际验证：类型检查、61/61 单元测试及生产构建通过。真实 Electron 使用合成凭据验证旧值替换、新值加密落盘、非法输入拒绝、重启后使用新值、401 与 404 分阶段提示，全程未写入外部仓库。
- 限制：没有证明用户当前 Token 有效；失效或被撤销的 Token 必须由用户在 GitHub 重新生成。私库检查只验证读取权限，不能证明写入权限。
- 测试包：`release/github-token-check-20260919/win-unpacked/XMemeory.exe`，打包后凭据保存/替换/重启/分阶段诊断专项通过。未签名，运行需保留整个目录。

## v0.2.0 图片复制到 X 的兼容修复（2026-09-19）

- 默认右键“复制图片”、预览复制按钮与 Ctrl+C 改为仅写入 PNG，移除默认 HTML 与纯文本回退，避免接收编辑器优先选择富文本/文字。
- 右键增加“复制图片（邮件富文本）”，显式保留 PNG 与内嵌图片 HTML。再次默认复制会替换旧剪贴板并清除 HTML；不修改本地图片或正文。
- 实际验证：类型检查、59/59 单元测试、生产构建通过；真实 Electron 图片专项验证默认仅 PNG、Chromium 粘贴事件得到有效 image/png 文件且无 HTML/文字、邮件模式 HTML、模式切换、预览与正文不变。
- 限制：未在真实 X 编辑器复现相机表情转换，不把浏览器粘贴事件测试等同于 X 验收；混合剪贴板格式是本轮定位到的兼容风险，需用户重试真实粘贴。
- 新测试包：`release/image-paste-x-20260919/win-unpacked/XMemeory.exe`，打包后的图片复制专项通过；需保留完整目录，未签名、未发布。

## v0.2.0 灵感图片预览与灵感内创作（2026-09-19）

- 灵感本地图片可双击打开大图，默认适配窗口，支持原始尺寸切换；可用 Esc、关闭按钮或点击遮罩退出。预览和复制不修改 Markdown 正文或保存状态。
- 保留正文原生右键“复制图片”，大图中同样支持右键复制，并提供复制按钮；复制继续包含 PNG 与邮件 HTML。主进程增加剪贴板失败处理，避免菜单回调产生未处理异常。
- 移除独立“创作”侧栏入口。灵感内点击“创作”会先保存，再将当前标题和正文发给配置的 DeepSeek；无 Key 时提示配置，不发请求。记忆和候选稿不能直接触发生成；已有候选稿保留，可从原灵感内列表和全局搜索访问。
- 私库设置增加只读“检查连接”；检查和同步前保存当前输入，避免使用旧 Token/仓库配置。细粒度 Token 的仓库访问失败提示包含 Resource owner、Repository access、Contents 权限指引。连接检查通过不代表写入权限已验证。
- 实际验证：类型检查、59/59 单元测试、生产构建通过。真实 Electron 图片测试覆盖双击放大、原图切换、三种关闭方式、正文/预览右键复制、PNG/HTML 剪贴板和正文不变；真实 Electron 创作测试用合成 API 验证 DeepSeek 请求、仅标题灵感、最新未等待自动保存正文、候选稿关联/搜索及非灵感来源拒绝；日历和记忆页回归通过。
- 限制：未使用真实 API Key 或 Token 做外部写入，实际模型服务与用户私库授权仍待用户验收。保留此前其他日期、拖动及图片复制改动；没有发布到代码仓库。
- 测试包：`release/inspiration-preview-20260919/win-unpacked/XMemeory.exe`；打包后图片预览/复制、灵感创作和无编辑退出验证通过，需保留整个目录，未签名。

## v0.2.0 验收修复 — 灵感图片复制到邮件（2026-09-16）

- 修复灵感本地图片复制后无法粘贴到部分邮件富文本编辑器的问题。此前剪贴板只写入 `image/png`，现在一次复制同时提供 PNG 位图、带内嵌 Data URL 的 HTML 图片和纯文本回退，邮件编辑器可按自身支持格式读取。
- 补齐图片右键操作：在灵感正文中的本地图片上右键会显示原生“复制图片”菜单，不再要求先左键选中后按 `Ctrl+C`；快捷键复制仍保留。
- 图片仍从资料库安全读取，限制为受支持的本地图片格式和 20 MiB，不暴露本地文件路径，也不依赖临时文件或网络地址。
- 实际验证：TypeScript 类型检查、完整单元测试 `59/59`、生产构建通过；独立真实 Electron 回归会真实右键图片并执行原生“复制图片”，确认剪贴板同时包含有效 `image/png` 与 `text/html`。
- 旧目录 `release/image-copy-mail-20260916-ready/win-unpacked/` 只包含快捷键复制修复，不包含右键菜单；请使用新目录 `release/image-right-click-mail-20260916-final/win-unpacked/`。新二进制已通过“右键图片 → 复制图片 → 检查 PNG 与邮件 HTML”的完整专项测试。因 Windows 安全软件锁定 EXE 后处理，本次关闭 ASAR 封装和图标/版本资源编辑，运行时须保留整个 `win-unpacked` 目录。

## v0.2.0 验收修复 — 灵感日期归属与跨天移动（2026-09-16）

- 修复通用“新灵感”沿用日历历史选中日期的问题：现在始终按本机当天创建，并自动把日历切回今天；日期卡片内的快速输入仍创建到该卡片日期。
- 灵感拖拽从同日排序扩展为跨天移动：可拖到目标日期的已有条目前，也可拖到日期空白区域并追加到末尾；拖动期间整列高亮目标日期。
- 跨天移动会持久化新的 `scheduledDate`，并同步归一化来源日和目标日的 `sortOrder`；正文、完成状态和原创建时间保持不变，保存失败会刷新为磁盘真实状态。
- 实际验证：日历定向测试 `4/4`、完整单元测试 `59/59`、TypeScript 类型检查、生产构建和真实 Electron 日历测试通过。桌面测试覆盖选中过其他日期后通用新增仍归今天、同日排序、跨天移动及重启保持。
- 已生成未压缩 Windows x64 二进制目录：`release/calendar-move-fix-20260916-final/win-unpacked/`，未生成 ZIP 或安装器；其中 `XMemeory.exe` 已通过打包后日历专项测试。Windows 安全软件阻止了最后的 EXE 图标/版本资源编辑，但不影响应用启动与功能。项目目录不是 Git 仓库，因此未提交或发布。

## v0.2.0 验收修复 — GitHub 仓库请求 404（2026-09-16）

- 修复私库信息请求末尾多余 `/`：现在使用 `/repos/owner/repo`，不再使用 `/repos/owner/repo/`。对 GitHub 公开测试仓库实测前者返回 200，后者返回 404。
- 同步失败信息区分读取仓库、读取分支、读取内容和写入内容，并分别提示 Token 无效、仓库访问不足、分支读取失败及仓库可能尚无提交。不会自动切换分支或创建远端内容来掩盖错误。
- 同步状态文件读取移入异常收尾范围，读取失败后可重新尝试，不会一直保持“同步中”。
- 实际验证：类型检查、57/57 单元测试、生产构建通过。新增模拟完整同步链路及 401/403/404/409 错误测试，验证 master 分支非强制更新。真实私库同步仍待用户验证；独立只读诊断进程未能解密应用保存的凭据，未输出凭据、未执行远端写入。
- 测试包：`release/github-404-fix-20260916/win-unpacked/XMemeory.exe`。打包后的记忆页、完整仓库地址保存与无编辑退出测试通过，需保留整个目录；未签名、未发布。

## v0.2.0 验收修复 — 图片保存与完整仓库 URL（2026-09-16）

- 图片下载改为通过受管 Chrome 的临时标签页读取响应，沿用浏览器网络路径；串行执行，限制图片域名、30 秒超时和 20 MiB，结束关闭标签页。未连接 Chrome 时明确报错。
- 修复图片链接本地化后正文缩短导致已有记忆不更新；保留“我的笔记”和已标记为用户编辑的正文，仅替换已知图片引用。没有数字账号 ID 时按实际文件目录计算相对路径。
- 记忆页显示图片已保存/失败数量，采集开始重置计数；失败保留远程引用并可重新采集。辅助元数据保存图片路径映射及脱敏错误，区分部分成功和全部失败。
- 私库设置与同步均接受完整 GitHub HTTPS URL（含 `.git`）或 `owner/repo`，自动规范化并保留用户分支；拒绝其他主机和嵌入凭据。采集辅助 JSON 纳入同步白名单。
- 实际验证：类型检查、51/51 单元测试、生产构建通过；正式 Chrome 使用合成响应完成图片读取、PNG 落盘、Markdown 相对路径和笔记保留测试；打包 EXE 的记忆页、完整 URL 保存、master 分支保留与无编辑退出测试通过。
- 测试包：`release/images-sync-fix-20260916/win-unpacked/XMemeory.exe`，需保留整个目录。未签名，未发布。
- 限制：未使用真实登录态或 Token 调用 X/GitHub；真实图片下载及私库同步仍需用户验收。已有开发记录中的全量书签扫描限制保持不变；用户提供的是数据仓库，不作为代码发布仓库。

## v0.2.0 创作 Agent — Jina 联网搜索与网页阅读（2026-09-15）

- 创作面板新增“智能联网”和“仅本地”模式；默认智能联网，模型只在需要最新事实、外部出处或本地素材不足时搜索，普通改写仍可直接使用本地资料。
- 新增 Jina Search 与 Jina Reader 工具，不依赖 Tavily、MCP、本机 CLI 或额外搜索 API Key；提供搜索、读取、过程状态及失败后本地降级。
- 每次创作最多 8 个 Agent 步骤、3 次搜索和 5 个网页；只允许读取本轮搜索返回的公开 HTTPS URL，拒绝凭据、本地地址、私网地址与保留地址，并限制超时、响应体和模型输入长度。
- 候选稿保留实际读取网页的标题、URL、站点和访问时间，编辑页可打开来源核验；本地 Note 来源保持兼容，网页正文全文不落盘。
- TypeScript 类型检查、新增联网定向测试 `4/4`、完整单元测试 `47/47`、生产构建与真实 Electron 日历/创作面板回归通过。当前环境访问 Jina 时 Windows TLS 握手失败，因此真实联网搜索仍需在用户网络环境中验收；失败时会自动退回本地素材。

## v0.2.0 测试包修复 — 发布时间、Todo 排序与一键采集（2026-09-15）


- X 记忆新增可兼容的 `publishedAt`，从 GraphQL `legacy.created_at` 读取并标准化；记忆列表与详情优先显示发推时间，旧记录重新采集时可在不覆盖用户正文的前提下补齐。
- 灵感日历新增同日 Todo 拖拽排序，把手触发后持久化 `sortOrder`；旧条目继续按创建时间稳定排列，首次拖动后完成顺序归一化，重启后保持。
- 记忆页保留“采集”和“暂停”：已登录直接采集，未登录自动打开普通 Chrome，用户登录并关闭窗口后自动续采；重复点击受流程锁保护，暂停可终止登录等待或采集。
- CDP Chrome 启动及采集时移动到屏幕外但保持有界面渲染；登录、失效或安全验证时恢复到屏幕内。设置页新增“显示 Chrome”。
- TypeScript 类型检查、完整单元测试 `42/42`、生产构建、真实 Electron 日历测试与记忆页测试均通过。真实 X 登录后自动续采与窗口位置仍需在用户账号环境人工验收。

## v0.2.0 测试包修复 — Article 内嵌 X 推文只保留链接（2026-09-15）

- 修复 Article 中内嵌 X 推文卡片被递归解析后，将作者、推文正文、图片和互动信息一并复制进长文的问题。
- DOM 采集现在会在进入卡片内部前识别 `/用户名/status/数字ID`，将整张卡片折叠为单个规范化 `https://x.com/.../status/...` 链接块；同一链接在多轮滚动快照中按 URL 去重。
- 普通正文链接、正文图片及其相对位置保持原逻辑。TypeScript 类型检查、定向测试 `9/9`、完整测试 `42/42`、Electron 生产构建均通过；真实已保存长文需要重新采集后才会更新。

## v0.2.0 测试包修复 — 灵感图片复制与采集计数（2026-09-15）

- 灵感编辑器中的本地图片现在可点击后按 `Ctrl+C`，以真实 PNG 图片写入 Windows 系统剪贴板，可直接粘贴到其他支持图片的应用；仍通过资料库安全路径读取，不向渲染进程开放任意文件。
- 两条定向测试内容不再一律显示为“保存 2 条”：采集结果区分新增、更新和未变化，`正文` 计数仅统计本次真正新增的记忆。已有 1 条、补抓 1 条时会显示新增 1 条。
- TypeScript 类型检查、完整单元测试 `41/41`、生产构建通过。真实 Electron 图片复制步骤通过；完整桌面测试随后在既有“备用：导入 Cookie 文件”文案定位处超时，该后续失败与本次修改无关。

## v0.2.0 测试包修复 — X Article 完整正文与排版（2026-09-15）

- Article DOM 采集改为只读取 `twitterArticleRichTextView` 或 `longformRichTextComponent`，不再把入口推文 `tweetText` 或整个外层卡片当作长文正文。
- 最多滚动并采集 40 轮，每轮保存当前标题、段落、列表、引用、代码、正文链接和图片块；通过相邻已知块合并虚拟渲染快照，保持图片在正文中的相对位置。
- 只保留正文节点内的行内链接，过滤 `/analytics`、Article 媒体查看页和 `/photo/` 页面导航，不再把页面全部链接追加到文末。
- Article 到达页面底部且连续两轮无新增块才标记 `complete`；否则记录 `article-not-at-bottom` 或 `article-content-unstable`。结构化元数据保存有序正文块，并区分 `cdp-dom` 与 `cdp-hybrid`。
- 增加多轮块合并、图片原位、正文链接及格式转换回归测试。TypeScript 类型检查、定向测试 `7/7`、完整测试 `40/40`、Electron 生产构建均通过；构建仅保留既有的 `x-network.ts` 动静态导入提示。真实 X Article 页面仍需用户重新采集两条白名单内容完成验收。

## v0.2.0 测试包修复 — 普通 Chrome 首次登录（2026-09-15）

- 修复 X 账号确认页输入手机号后点击“继续”无响应：首次登录改用完全普通的 Chrome，不携带远程调试参数，也不连接 Playwright。
- 用户在普通 Chrome 完成登录后关闭该窗口，再点击“检查登录”；应用随后使用同一专属 Profile 启动 CDP，只在检查和定向采集阶段启用远程调试。
- 增加启动参数隔离测试，确保登录模式不含任何 `--remote-debugging-*` 参数，采集模式仍只监听 `127.0.0.1` 临时端口。类型检查、完整测试 `38/38`、生产构建和打包 EXE 启动回归通过；真实 X 登录需用户人工验收。

## v0.2.0 测试包修复 — Chrome 登录与 SQLite 自动恢复（2026-09-15）

- Chrome 专属 Profile 启动时改为打开空白页；点击登录后只进入 X 官方 `/i/flow/login`，不再在登录阶段提前访问测试推文。识别到 `auth_token` 后回到空白页，只有点击采集时才访问两条白名单内容。
- 修复损坏的 `local/index.sqlite` 导致应用在创建窗口前退出：启动时执行快速完整性检查，确认损坏后将数据库及 WAL/SHM 保留为带时间戳的 `.corrupt-*` 副本，新建索引并从 Markdown 权威正文自动重建。
- 新增数据库损坏恢复回归测试；类型检查、完整测试 `37/37`、生产构建和打包 EXE 记忆页启动回归均通过。真实 X 登录仍需用户在 Chrome 中人工验收。
- 本机安全软件持续拦截 ASAR 打包阶段对 EXE 的完整性写入；本次改为生成 `asar=false` 的完整免安装目录，不影响运行，但必须保留整个目录，不能只复制 EXE。

## v0.2.0 第一阶段 — CDP 两条定向保存（2026-09-15）

当前为本地开发验收版，未发布。为避免首版误触批量采集，本阶段只允许保存用户指定的两条 X 内容：`xueyu1125/2099104432596328908` 与 `xueyu1125/2099446491094175744`。

- 新增正式版 Chrome 独立 Profile 与 Playwright `connectOverCDP()` 会话；登录态由 Chrome 保存，不读取日常 Chrome Profile，也不向资料库写入裸 Cookie。
- 首次登录、安全验证由用户在 Chrome 中完成。采集时使用有界面 Chrome，窗口移到屏幕外；应用启动不会自动弹出 Chrome。
- 删除界面和 IPC 中的批量“扫描书签”入口，关闭旧自动采集设置与定时任务；采集器只接受代码内两个精确白名单 URL，不接受任意 URL。
- 依次访问两条状态页，只消费页面自然产生的 GraphQL 响应；若识别到 X Article 且响应仅含预览，会进入真实 Article 页面继续等待响应和滚动渲染。
- 保存 Markdown 和 `metadata/<noteId>.capture.json`。Markdown 分为“X 原文”和“我的笔记”；结构化 JSON 记录正文块、链接、图片、视频标记、采集方式与完整性状态。
- 图片通过当前 Chrome 上下文低并发下载到 `assets/`，成功后改为相对路径；失败继续保留远程 URL。视频不下载。
- 结构化 Article 块存在时按完整正文处理；仅预览或 DOM 降级时标记 `partial`，不会把空正文写入资料库。
- 新增 4 个定向采集测试，覆盖 URL 白名单、短推、Article 块和预览不完整判定。最终类型检查通过，核心测试 `36/36` 通过，Electron 生产构建通过；构建仍有既存的 `x-network.ts` 动静态导入提示，不影响构建。
- 未完成：尚未使用用户真实 X 登录态访问这两条内容，因此真实页面 schema、Article 完整段落、图片下载和 Windows Chrome 窗口行为仍需人工验收。未生成安装包，未执行 Git 提交、推送或 tag；当前目录不是 Git 仓库。

## v0.1.0 开发修复 — X Article 长文与封面（2026-09-15）

- 长文拉不到：书签时间线对 X Article 往往只有 t.co / 标题预览，`plain_text` 为空。现从 `article.content.blocks` 拼正文，并从 `cover_media.original_img_url` 取封面；不足时再请求 `TweetResultByRestId`。
- 实测 `xueyu1125/2099446491094175744` 是 X Article（`/i/article/2099421537489100800`，正文 37 段、封面 `pbs.twimg.com/media/HSKk1VaasAA8HOf.jpg`）。`PandaTalk8/2097877140268356021` 是短提问，不是长文/Article，书签里本来就没有更长正文。
- 封面此前失败叠加 302/AVIF/` :large` 问题；Article 封面走同一套 JPEG 规范化下载。合成时间线 + 补取测试通过。

## v0.1.0 开发修复 — 记忆页登录态与图片下载（2026-09-15）

- 登录成功后记忆右侧仍显示「登录 X 书签」，因未读取登录态。现根据 `xLoggedIn` 切换：未登录显示「登录 X」，已登录显示「扫描书签」；记忆工具栏同样提供「登录 X」。
- 附件仍失败的完整原因：1) `pbs.twimg.com` 302 且 Electron `session.fetch` 默认禁止跳转；2) `Accept: */*` 常拿到 AVIF，本地不识别；3) 旧地址 `:large` 后缀未规范化。下载改为 JPEG 优先、跟随跳转、规范化 `format/name`，并尊重 `credentials: omit`。

## v0.1.0 开发修复 — 附件失败数偏高（2026-09-15）

- 「失败 295」是图片/封面下载失败，不是书签正文失败。pbs.twimg.com 常 302 到 `name=large`，原下载禁止跳转导致几乎全部附件失败。
- 下载改为跟随有限次重定向、补全 `name=large`，并按帖并行（最多 3 路）缩短等待。界面标明「附件失败」，正文仍保留，可重试。

## v0.1.0 开发修复 — 扫描 404（2026-09-15）

- 扫描原先先打已失效的 `verify_credentials`，404 会直接中断。改为用 `twid` Cookie 识别账号；账号资料接口失败不再阻止扫描。
- 书签请求复用登录窗口捕获的真实 GraphQL（含 POST、features、api.x.com），GET 404 时自动改试 POST 与备用域名。

## v0.1.0 开发修复 — 登录成功后账号接口失败（2026-09-15）

- 登录 Cookie 已拿到后，主进程 `session.fetch` 访问 `verify_credentials` 常因重定向到 `api.x.com` 或不走页面上下文而失败。改为在已登录的隐藏窗口内用页面 `fetch` 发接口，并允许 `api.x.com`。
- 登录成功不再被后续账号校验失败整段推翻：会话保留，可继续扫描；校验失败会单独说明。
- 登录后多等几秒以捕获书签 GraphQL；重启后自动打开隐藏会话窗口。

## v0.1.0 方案变更 — 应用内登录窗口（系统代理）（2026-09-15）

- Playwright 新开浏览器未走系统代理，登录页无响应。改为 Electron 持久化分区登录窗口：与采集共用会话，强制系统代理，重启后仍记住登录态。
- 不读取日常 Chrome Cookie 数据库（Windows 应用绑定加密，外部进程无法稳定解密）。
- Google/Apple 网页授权可能被内嵌窗口拒绝，界面提示优先用 X 账号密码。Cookie 文件导入仍为备用并写入同一分区。25 个核心测试与桌面回归通过。打包输出 `release/proxy-login-20260915/win-unpacked/`。

## v0.1.0 开发修复 — 识别当前 X 书签客户端（2026-09-14）

- 验证登录时原先只请求 `https://x.com`。该地址现已改为 x-web 未登录落地页，不再包含 `client-web/main.*.js`，因此出现“无法识别 X 当前客户端”。
- 改为优先读取 `https://x.com/i/bookmarks`，从 webpack 运行时定位 `shared~bundle.BookmarkFolders~bundle.Bookmarks` 分包并提取 `Bookmarks` queryId；请求带浏览器 User-Agent。未登录落地页会给出明确说明，不再误报“找不到 main.js”。
- 合成页面与分包识别测试通过；本机已对公开 `/i/bookmarks` 页面确认仍能定位 Bookmarks 分包。真实账号登录仍需用新包验证。

## v0.1.0 开发阶段 — 记忆归档与阅读（2026-09-14）

当前为记忆功能的本地验收版，非完整首版发布。用户此前已确认灵感验收完成，并要求先解除 `fetch failed`、修复无编辑退出误报，再做记录功能。

- 记忆页展示采集阶段、发现数、已保存正文、图片成功/失败数和断点提示；可筛选作者、关键词、有图/仅正文/不完整，并在列表与卡片间切换。
- 打开归档可查看 Markdown 正文与图片，提供打开原文、复制链接、加入创作素材；不完整正文和未下载视频会在正文中标明。
- 采集将书签按账号分层写入 `memories/<accountId>/<postId>.md`，保存正文、外链、封面和图片；不下载视频流，不写入点赞/评论等互动字段。历史游标仅在整页正文写入后推进；中断后从该游标继续。用户改过的 Markdown 不会被再次扫描覆盖。
- 图片失败保留正文并进入补抓队列，可单独“重试失败附件”。完成后的增量扫描从最新页重新遍历，避免因遇到已有记录而漏掉新收藏的旧推文。
- 无编辑直接关闭的误报修复已纳入本包：浏览日历或设置后关闭不再弹出恢复对话框。
- 验证：20 个核心测试（含 5 个合成书签归档用例）通过；真实 Electron 记忆阅读测试和无编辑退出测试通过。类型检查与构建通过。已生成 `release/memory-20260914/win-unpacked/`，并对该 EXE 重跑记忆阅读与无编辑退出测试，均通过。真实 X 登录、限速下的全量历史、Mac 验收未在本轮执行。

## v0.1.0 开发修复 — 未编辑时误弹保存警告（2026-09-14）

- 原关闭流程无论是否编辑都向界面请求保存，并将任何失败（含确认超时）统一解释为未保存正文。本次增加正文修改状态通报；没有未保存修改时直接关闭，不再等待无必要的保存握手。
- 仅正文/标题实际变化标记未保存，保存成功清除状态，浏览日历、设置、源码预览切换不标记；恢复副本成功保存后允许继续关闭。
- 保存握手加入请求 ID，过时响应不影响新请求；真实保存失败继续提供恢复副本，响应超时改为独立的“窗口暂未响应”提示。
- 新增真实 Electron 测试：无编辑日历、仅浏览设置、已保存正文切换预览后关闭，均无恢复弹窗且不创建恢复副本。常规桌面编辑与退出冲突恢复回归通过。
- 该修复不修改用户正文、Cookie 或系统代理；网络修复仍包含在新包中。

## v0.1.0 开发修复 — X 请求使用系统代理（2026-09-14）

- 用户已确认灵感验收完成，转入记忆功能验收。
- 定位导入 Cookie 后的 `fetch failed`：当前机器启用系统代理，原 Node fetch 请求公开 X 首页得到 `UND_ERR_CONNECT_TIMEOUT`；Chromium 请求得到 HTTP 200。
- X 适配器改为独立 Electron 网络会话，使用系统网络与代理。保留 HTTPS 主机限制、30 秒超时、显式 Cookie、禁止重定向和正常证书校验。没有读取/输出用户 Cookie 或修改用户代理设置。
- 增加连接阶段提示、公开首页和客户端配置的 HTTP 状态检查；网络错误显示 DNS、代理、超时或证书原因，不展示底层敏感错误正文，也不误报 Cookie 失效。
- 类型检查、构建与 14 个核心测试通过。使用本机合成 HTTP 服务在 Electron 中验证显式 Cookie 被发送、会话 Cookie 不混入、重定向被阻止。
- 测试公开请求不携带 Cookie；真实账号登录与书签采集完整性仍未验证。联网探测脚本 `tests/network-smoke.ts` 不纳入默认离线测试。
- 打包输出 `release/network-20260914/win-unpacked/`；打包 EXE 的 Cookie 传输隔离测试、公开网络测试和灵感日历回归通过。最终公开探测：Node 请求 `ECONNRESET`，应用同分区 Chromium 会话使用系统代理得到 HTTP 200。

## v0.1.0 开发阶段 — 灵感周/月日历（2026-09-14）

当前为灵感功能的本地验收版，非完整首版发布。

- 灵感默认展示本周七天，可翻周、回到本周，或选择任意整月；每个日期可输入标题并按 Enter 快速创建。空标题不创建，中文输入法确认候选时不提前提交。
- 一条灵感就是一条可勾选的 Todo；勾选后标题划线，留在原日并可撤销。未使用条目不自动顺延、不算逾期、不提醒。点击标题打开 Markdown 编辑器，返回时保留日历位置。
- 日期和完成状态写入 Markdown Front Matter；重启保留，支持外部修改后回读。旧条目缺少日期时按创建日期显示，缺少完成状态时视为未完成。
- 新增插入链接表单，完整域名自动补 HTTPS，不明确或危险地址提示修正；手动输入标准 HTTPS Markdown 链接可立即转换。
- “加入素材”改为“加入创作素材”，先保存再打开创作面板，显示数量与说明，重复添加去重、点击素材可移除。不会自动生成或标完成，素材选择仅在本次会话保留。
- 验证：13 个核心测试、类型检查、构建通过；常规 Electron 编辑/图片/搜索测试、日历专项桌面测试、单实例与退出恢复测试通过。日历专项含闰日、重启、完成撤销、外部日期和完成状态回读、手动链接及素材去重移除。
- 已查看周/月视图截图。真实 X、DeepSeek、GitHub、Mac、签名验收仍未完成；素材会话持久化、日历拖拽改期和提醒不在本次交付范围。
- 已生成独立验收目录 `release/calendar-20260914/win-unpacked/`，对其中 EXE 重跑日历专项、常规编辑与退出恢复测试，均通过；不覆盖旧包，不执行 Git 发布。

## v0.1.0 开发修复 — 数据库锁与无法关闭（2026-09-14）

- 用户截图显示文件监听刷新抛出未捕获的 `database is locked`；进程检查发现两个不同构建目录的 App 同时运行。
- 将索引初始化和完整事务纳入错误处理，事务开始即失败时不遗留事务。锁冲突短暂等待后最多异步重试 3 次，界面提示占用；Markdown 保存和当前内存搜索不因索引失败被误报为失败。监听错误不再成为主进程未捕获异常。
- 在打开资料库前获取 Electron 单实例锁，重复启动退出并唤醒已有窗口。旧构建未带此保护，升级前仍需退出旧包。
- 保存冲突时提供另存恢复副本后关闭的路径；副本存于应用数据目录 `recovery/`，原文和冲突保留，下次启动提示恢复目录。未删除用户数据，也未强制结束用户进程。
- 类型检查、10 个核心用例和构建通过。新增独立 SQLite 连接持锁测试：锁住期间正文可保存，解锁后索引自动恢复，持锁期间新建资料库连接也不抛异常。
- `pnpm exec tsx tests/desktop-recovery.ts` 通过：重复启动拦截、唤醒已有窗口、保存冲突后另存完整副本再关闭、原文和冲突保留。常规桌面用例通过。

## v0.1.0 开发阶段 — 灵感编辑与接入骨架（2026-09-13）

当前为本地验收版，尚未连接或发布 GitHub 仓库。

- 初始化 Electron + React + TypeScript 桌面项目，生产窗口加载本地构建文件；建立 `inspirations/`、`memories/`、`drafts/`、`assets/`、`metadata/`、`tombstones/`、`conflicts/`、`local/` 资料库结构。
- 灵感支持 Markdown 兼容编辑、标题与正文自动保存、源码/正文切换、表格、网页链接、`[[双链]]` 和笔记摘要嵌入；图片粘贴写入 `assets/`，Markdown 使用相对路径，重启后可显示。
- 外部 Markdown 修改自动回读；保存时使用期望哈希防覆盖，并生成可比较冲突，支持保留当前、采用外部、保留双方或手动合并。删除写入 tombstone，回收站可恢复。
- 全局搜索覆盖灵感、记忆、候选稿正文、标签、作者和来源；支持类型、作者、标签、日期筛选、相关度/时间排序。本机 SQLite FTS 索引可重建，搜索不调用模型。
- X 接入实现 Cookie 文本/JSON 导入、目录监听、凭据加密存储、基础验证、串行限速、429 冷却、历史游标和图片失败队列；实际 GraphQL endpoint 与真实账号尚未联调。
- DeepSeek 接入支持流式短推/串文、停止生成、保存候选稿和来源；GitHub 私库同步支持 allowlist、拉取合并、冲突保留和推送。两项均需用户凭据和真实环境验收。
- 桌面体验支持主题、托盘驻留、窗口关闭前刷盘、资料库切换与打开、图片和 Markdown 导出。
- 验证：`pnpm typecheck`、`pnpm test`、`pnpm build` 通过；共 8 个核心测试。真实 Electron 窗口完成新建、编辑、图片粘贴、重启显示、搜索、外部修改回读、表格、链接、双链、冲突保留和回收站恢复测试，且无未捕获页面错误。
- Windows 已执行 `pnpm pack:win`，产出 NSIS 安装包和 `win-unpacked`；Mac 打包只能在 Mac 环境执行。未配置代码签名、公证与自动更新。

## v0.1.0 规划阶段（2026-09-13）

- 已创建并确认首个可用版本详细设计：Electron + React + TypeScript，Markdown 为权威正文，本地 SQLite FTS 可重建索引。
- 已确认范围：灵感、X 书签记忆、DeepSeek 短推/串文创作、用户直接使用的全局搜索、GitHub 私有资料库同步、浅色/深色/跟随系统、托盘驻留与快捷键。
- 已确认数据边界：正文优先，保存图片/封面与外链，不保存评论/点赞；视频不下载；Article 作为收藏主体时补取正文；Cookie 无法直接读取时本地导入。
- 已确认一致性策略：Markdown 正文允许外部编辑并回读；GitHub 冲突保留双方，不静默覆盖；密钥和 Cookie 仅本机加密保存，不进入资料库或 Git。
- 尚未进行功能开发、真实平台接入、跨平台打包、签名与发布。
